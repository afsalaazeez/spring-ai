/*
 * Copyright 2023-2024 the original author or authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test spring-ai-test-timeout-pr using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=getUserText_1961a835a5
ROOST_METHOD_SIG_HASH=getUserText_15292a40d4

Scenario 1: Retrieve User Text When Initialized With Non-Empty String

Details:
  TestName: getUserTextReturnsNonEmptyStringWhenInitializedWithUserText
  Description: This test verifies that the getUserText method correctly returns the user text value when the EvaluationRequest object is created with a non-empty user text string using the three-parameter constructor.

Execution:
  Arrange: Create an EvaluationRequest instance with a non-empty user text string, an empty list of documents, and a response content string.
  Act: Invoke the getUserText method on the created EvaluationRequest instance.
  Assert: Verify that the returned value equals the user text string that was passed during construction.

Validation:
  The assertion confirms that the getUserText method correctly retrieves and returns the userText field value. This is essential for ensuring that the evaluation request properly stores and provides access to the user's input text, which is fundamental for evaluating chat responses against user queries.

---

Scenario 2: Retrieve User Text When Initialized With Empty String Using Two-Parameter Constructor

Details:
  TestName: getUserTextReturnsEmptyStringWhenInitializedWithDataListConstructor
  Description: This test verifies that the getUserText method returns an empty string when the EvaluationRequest is created using the constructor that only accepts dataList and responseContent parameters.

Execution:
  Arrange: Create an EvaluationRequest instance using the two-parameter constructor with a list of documents and a response content string.
  Act: Invoke the getUserText method on the created EvaluationRequest instance.
  Assert: Verify that the returned value is an empty string.

Validation:
  The assertion confirms that when using the constructor without explicit userText, the default empty string is properly set and returned. This validates the constructor's default behavior and ensures consistent handling of cases where user text is not provided.

---

Scenario 3: Retrieve User Text When Initialized With UserText And ResponseContent Constructor

Details:
  TestName: getUserTextReturnsCorrectValueWhenInitializedWithUserTextAndResponseContent
  Description: This test verifies that the getUserText method correctly returns the user text when the EvaluationRequest is created using the two-parameter constructor that accepts userText and responseContent.

Execution:
  Arrange: Create an EvaluationRequest instance using the constructor with userText and responseContent parameters.
  Act: Invoke the getUserText method on the created EvaluationRequest instance.
  Assert: Verify that the returned value matches the userText provided during construction.

Validation:
  The assertion validates that the simplified constructor properly initializes the userText field and that getUserText retrieves it correctly. This is important for scenarios where only user text and response content are needed without additional document data.

---

Scenario 4: Retrieve User Text When Initialized With Null Value

Details:
  TestName: getUserTextReturnsNullWhenInitializedWithNullUserText
  Description: This test verifies the behavior of getUserText when the EvaluationRequest is created with a null userText value.

Execution:
  Arrange: Create an EvaluationRequest instance with null as the userText parameter, an empty list for dataList, and a valid responseContent string.
  Act: Invoke the getUserText method on the created EvaluationRequest instance.
  Assert: Verify that the returned value is null.

Validation:
  The assertion confirms that the getUserText method returns null when null was passed during construction. This tests the edge case handling and ensures that the method does not perform any transformation on null values, which is important for downstream null-checking logic.

---

Scenario 5: Retrieve User Text With Special Characters

Details:
  TestName: getUserTextReturnsStringWithSpecialCharacters
  Description: This test verifies that the getUserText method correctly returns user text containing special characters, unicode characters, and escape sequences.

Execution:
  Arrange: Create an EvaluationRequest instance with a userText string containing special characters such as newlines, tabs, quotes, and unicode characters.
  Act: Invoke the getUserText method on the created EvaluationRequest instance.
  Assert: Verify that the returned value exactly matches the original string with all special characters preserved.

Validation:
  The assertion ensures that the getUserText method preserves all special characters without modification. This is critical for maintaining data integrity when processing user inputs that may contain various character types.

---

Scenario 6: Retrieve User Text With Very Long String

Details:
  TestName: getUserTextReturnsVeryLongString
  Description: This test verifies that the getUserText method correctly handles and returns a very long user text string without truncation or modification.

Execution:
  Arrange: Create an EvaluationRequest instance with a very long userText string (e.g., several thousand characters).
  Act: Invoke the getUserText method on the created EvaluationRequest instance.
  Assert: Verify that the returned value has the same length and content as the original long string.

Validation:
  The assertion confirms that there are no length limitations or truncation issues with the getUserText method. This is important for applications that may process lengthy user queries or documents.

---

Scenario 7: Retrieve User Text With Whitespace Only String

Details:
  TestName: getUserTextReturnsWhitespaceOnlyString
  Description: This test verifies that the getUserText method correctly returns a string containing only whitespace characters.

Execution:
  Arrange: Create an EvaluationRequest instance with a userText string containing only spaces, tabs, or other whitespace characters.
  Act: Invoke the getUserText method on the created EvaluationRequest instance.
  Assert: Verify that the returned value matches the whitespace-only string exactly.

Validation:
  The assertion ensures that whitespace-only strings are handled correctly without being trimmed or converted to empty strings. This validates that the method performs no implicit string manipulation.

*/

// ********RoostGPT********

package org.springframework.ai.evaluation;

import java.util.Collections;
import java.util.List;
import java.util.ArrayList;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.ai.document.Document;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import java.util.Objects;

class EvaluationRequestGetUserTextTest {

	@Test
	@Tag("valid")
	void getUserTextReturnsNonEmptyStringWhenInitializedWithUserText() {
		// Arrange
		String userText = "What is the capital of France?";
		List<Document> dataList = Collections.emptyList();
		String responseContent = "The capital of France is Paris.";
		EvaluationRequest evaluationRequest = new EvaluationRequest(userText, dataList, responseContent);
		// Act
		String result = evaluationRequest.getUserText();
		// Assert
		assertEquals(userText, result);
	}

	@Test
	@Tag("valid")
	void getUserTextReturnsEmptyStringWhenInitializedWithDataListConstructor() {
		// Arrange
		List<Document> dataList = new ArrayList<>();
		dataList.add(new Document("Sample document content"));
		String responseContent = "This is a response.";
		EvaluationRequest evaluationRequest = new EvaluationRequest(dataList, responseContent);
		// Act
		String result = evaluationRequest.getUserText();
		// Assert
		assertEquals("", result);
	}

	@Test
	@Tag("valid")
	void getUserTextReturnsCorrectValueWhenInitializedWithUserTextAndResponseContent() {
		// Arrange
		String userText = "Tell me about machine learning.";
		String responseContent = "Machine learning is a subset of artificial intelligence.";
		EvaluationRequest evaluationRequest = new EvaluationRequest(userText, responseContent);
		// Act
		String result = evaluationRequest.getUserText();
		// Assert
		assertEquals(userText, result);
	}

	@Test
	@Tag("boundary")
	void getUserTextReturnsNullWhenInitializedWithNullUserText() {
		// Arrange
		String userText = null;
		List<Document> dataList = Collections.emptyList();
		String responseContent = "Some response content.";
		EvaluationRequest evaluationRequest = new EvaluationRequest(userText, dataList, responseContent);
		// Act
		String result = evaluationRequest.getUserText();
		// Assert
		assertNull(result);
	}

	@Test
	@Tag("valid")
	void getUserTextReturnsStringWithSpecialCharacters() {
		// Arrange
		String userText = "Hello\nWorld\t\"Special\" chars: é, ñ, 中文, 日本語, \u2603";
		List<Document> dataList = Collections.emptyList();
		String responseContent = "Response with special characters.";
		EvaluationRequest evaluationRequest = new EvaluationRequest(userText, dataList, responseContent);
		// Act
		String result = evaluationRequest.getUserText();
		// Assert
		assertEquals(userText, result);
		assertTrue(result.contains("\n"));
		assertTrue(result.contains("\t"));
		assertTrue(result.contains("\""));
		assertTrue(result.contains("é"));
		assertTrue(result.contains("中文"));
	}

	@Test
	@Tag("boundary")
	void getUserTextReturnsVeryLongString() {
		// Arrange
		StringBuilder longStringBuilder = new StringBuilder();
		for (int i = 0; i < 10000; i++) {
			longStringBuilder.append("This is a very long user text. ");
		}
		String userText = longStringBuilder.toString();
		List<Document> dataList = Collections.emptyList();
		String responseContent = "Response to long text.";
		EvaluationRequest evaluationRequest = new EvaluationRequest(userText, dataList, responseContent);
		// Act
		String result = evaluationRequest.getUserText();
		// Assert
		assertEquals(userText, result);
		assertEquals(userText.length(), result.length());
	}

	@Test
	@Tag("boundary")
	void getUserTextReturnsWhitespaceOnlyString() {
		// Arrange
		String userText = "   \t\t   \n\n   ";
		List<Document> dataList = Collections.emptyList();
		String responseContent = "Response content.";
		EvaluationRequest evaluationRequest = new EvaluationRequest(userText, dataList, responseContent);
		// Act
		String result = evaluationRequest.getUserText();
		// Assert
		assertEquals(userText, result);
		assertEquals("   \t\t   \n\n   ", result);
	}

	@Test
	@Tag("boundary")
	void getUserTextReturnsEmptyStringWhenInitializedWithEmptyString() {
		// Arrange
		String userText = "";
		List<Document> dataList = Collections.emptyList();
		String responseContent = "Some response.";
		EvaluationRequest evaluationRequest = new EvaluationRequest(userText, dataList, responseContent);
		// Act
		String result = evaluationRequest.getUserText();
		// Assert
		assertEquals("", result);
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("valid")
	void getUserTextReturnsSameReferenceAsInitialized() {
		// Arrange
		String userText = "Test user text";
		EvaluationRequest evaluationRequest = new EvaluationRequest(userText, "response");
		// Act
		String result = evaluationRequest.getUserText();
		// Assert
		assertSame(userText, result);
	}

	@Test
	@Tag("valid")
	void getUserTextIsConsistentAcrossMultipleCalls() {
		// Arrange
		String userText = "Consistent user text";
		EvaluationRequest evaluationRequest = new EvaluationRequest(userText, Collections.emptyList(), "response");
		// Act
		String result1 = evaluationRequest.getUserText();
		String result2 = evaluationRequest.getUserText();
		String result3 = evaluationRequest.getUserText();
		// Assert
		assertEquals(result1, result2);
		assertEquals(result2, result3);
		assertSame(result1, result2);
		assertSame(result2, result3);
	}

}