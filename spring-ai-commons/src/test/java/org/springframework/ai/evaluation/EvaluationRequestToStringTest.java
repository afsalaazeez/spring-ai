/*
 * Copyright 2023-2024 the original author or authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test spring-ai-test-timeout-pr using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=toString_951497790c
ROOST_METHOD_SIG_HASH=toString_ceffa8036e

Here are your existing test cases which we found out and are not considered for test generation:

File Path: /var/tmp/Roost/RoostGPT/spring-ai-test-timeout-pr/1e559eba-5752-4feb-88f7-63ae643c0140/source/spring-ai/spring-ai-commons/src/test/java/org/springframework/ai/document/id/JdkSha256HexIdGeneratorTest.java
Tests:
    "@Test
void messageDigestReturnsDistinctInstances() {
    final MessageDigest md1 = this.testee.getMessageDigest();
    final MessageDigest md2 = this.testee.getMessageDigest();
    Assertions.assertThat(md1 != md2).isTrue();
    Assertions.assertThat(md1.getAlgorithm()).isEqualTo(md2.getAlgorithm());
    Assertions.assertThat(md1.getDigestLength()).isEqualTo(md2.getDigestLength());
    Assertions.assertThat(md1.getProvider()).isEqualTo(md2.getProvider());
    Assertions.assertThat(md1.toString()).isEqualTo(md2.toString());
}
"

File Path: /var/tmp/Roost/RoostGPT/spring-ai-test-timeout-pr/1e559eba-5752-4feb-88f7-63ae643c0140/source/spring-ai/spring-ai-commons/src/test/java/org/springframework/ai/document/DocumentTests.java
Tests:
    "@Test
void testToString() {
    Media media = getMedia();
    Map<String, Object> metadata = new HashMap<>();
    metadata.put("key", "value");
    Double score = 0.95;
    Document document = Document.builder().id("customId").text("Test content").media(null).metadata(metadata).score(score).build();
    String toString = document.toString();
    assertThat(toString).contains("id='customId'").contains("text='Test content'").contains("metadata=" + metadata).contains("score=" + score);
}
"

File Path: /var/tmp/Roost/RoostGPT/spring-ai-test-timeout-pr/1e559eba-5752-4feb-88f7-63ae643c0140/source/spring-ai/spring-ai-commons/src/test/java/org/springframework/ai/writer/FileDocumentWriterTest.java
Tests:
    "@Test
void setUp() {
    this.testFileName = this.tempDir.resolve("file-document-test-output.txt").toString();
    this.testDocuments = List.of(Document.builder().text("Document one introduces the core functionality of Spring AI.").metadata("page_number", "1").metadata("end_page_number", "2").metadata("source", "intro.pdf").metadata("title", "Spring AI Overview").metadata("author", "QA Team").build(), Document.builder().text("Document two illustrates multi-line handling and line breaks.\nEnsure preservation of formatting.").metadata("page_number", "3").metadata("end_page_number", "4").metadata("source", "formatting.pdf").build(), Document.builder().text("Document three checks metadata inclusion and output formatting behavior.").metadata("page_number", "5").metadata("end_page_number", "6").metadata("version", "v1.2").build());
}
"
    "@Test
void testMetadataModeAllWithDocumentMarkers() throws IOException {
    var writer = new FileDocumentWriter(this.testFileName, true, MetadataMode.ALL, false);
    writer.accept(this.testDocuments);
    List<String> lines = Files.readAllLines(Path.of(this.testFileName));
    assertEquals("", lines.get(0));
    assertEquals("### Doc: 0, pages:[1,2]", lines.get(1));
    String subListToString = lines.subList(2, 7).toString();
    assertTrue(subListToString.contains("page_number: 1"));
    assertTrue(subListToString.contains("end_page_number: 2"));
    assertTrue(subListToString.contains("source: intro.pdf"));
    assertTrue(subListToString.contains("title: Spring AI Overview"));
    assertTrue(subListToString.contains("author: QA Team"));
    assertEquals("", lines.get(7));
    assertEquals("Document one introduces the core functionality of Spring AI.", lines.get(8));
    assertEquals("### Doc: 1, pages:[3,4]", lines.get(9));
    subListToString = lines.subList(10, 13).toString();
    assertTrue(subListToString.contains("page_number: 3"));
    assertTrue(subListToString.contains("source: formatting.pdf"));
    assertTrue(subListToString.contains("end_page_number: 4"));
    assertEquals("", lines.get(13));
    assertEquals("Document two illustrates multi-line handling and line breaks.", lines.get(14));
    assertEquals("Ensure preservation of formatting.", lines.get(15));
    assertEquals("### Doc: 2, pages:[5,6]", lines.get(16));
    subListToString = lines.subList(17, 20).toString();
    assertTrue(subListToString.contains("page_number: 5"));
    assertTrue(subListToString.contains("end_page_number: 6"));
    assertTrue(subListToString.contains("version: v1.2"));
    assertEquals("", lines.get(20));
    assertEquals("Document three checks metadata inclusion and output formatting behavior.", lines.get(21));
}
"Scenario 1: Verify toString with all fields populated

Details:
  TestName: toStringWithAllFieldsPopulated
  Description: Verify that the toString method correctly formats the output string when all fields (userText, dataList, and responseContent) are populated with valid values.
Execution:
  Arrange: Create an EvaluationRequest instance with a non-empty userText, a list containing one or more Document objects, and a non-empty responseContent.
  Act: Invoke the toString() method on the EvaluationRequest instance.
  Assert: Verify that the returned string contains "EvaluationRequest{", "userText='<value>'", "dataList=<list>", "chatResponse=<value>", and ends with "}".
Validation:
  The assertion verifies that all three fields are properly included in the string representation with correct formatting. This is important for debugging and logging purposes to ensure complete visibility of the object's state.

Scenario 2: Verify toString with empty userText

Details:
  TestName: toStringWithEmptyUserText
  Description: Verify that the toString method correctly handles an empty string for userText while other fields are populated.
Execution:
  Arrange: Create an EvaluationRequest instance using the constructor that accepts dataList and responseContent (which sets userText to empty string).
  Act: Invoke the toString() method on the EvaluationRequest instance.
  Assert: Verify that the returned string contains "userText=''" indicating an empty userText field.
Validation:
  The assertion confirms that empty strings are properly represented in the toString output. This ensures that the method handles edge cases where userText is intentionally left empty.

Scenario 3: Verify toString with empty dataList

Details:
  TestName: toStringWithEmptyDataList
  Description: Verify that the toString method correctly handles an empty dataList when using the constructor that only accepts userText and responseContent.
Execution:
  Arrange: Create an EvaluationRequest instance using the constructor with userText and responseContent parameters (which sets dataList to Collections.emptyList()).
  Act: Invoke the toString() method on the EvaluationRequest instance.
  Assert: Verify that the returned string contains "dataList=[]" indicating an empty list.
Validation:
  The assertion verifies that an empty list is properly represented in the string output. This is crucial for scenarios where no context documents are provided for evaluation.

Scenario 4: Verify toString with null responseContent

Details:
  TestName: toStringWithNullResponseContent
  Description: Verify that the toString method correctly handles a null value for responseContent.
Execution:
  Arrange: Create an EvaluationRequest instance with valid userText, a valid dataList, and null for responseContent.
  Act: Invoke the toString() method on the EvaluationRequest instance.
  Assert: Verify that the returned string contains "chatResponse=null".
Validation:
  The assertion confirms that null values are properly represented in the toString output without causing NullPointerException. This is important for robustness when dealing with incomplete data.

Scenario 5: Verify toString with null userText

Details:
  TestName: toStringWithNullUserText
  Description: Verify that the toString method correctly handles a null value for userText.
Execution:
  Arrange: Create an EvaluationRequest instance with null for userText, a valid dataList, and valid responseContent.
  Act: Invoke the toString() method on the EvaluationRequest instance.
  Assert: Verify that the returned string contains "userText='null'" or handles the null appropriately.
Validation:
  The assertion verifies that null userText is handled gracefully in the string representation. This ensures the method doesn't throw exceptions when dealing with null input.

Scenario 6: Verify toString with multiple documents in dataList

Details:
  TestName: toStringWithMultipleDocumentsInDataList
  Description: Verify that the toString method correctly represents a dataList containing multiple Document objects.
Execution:
  Arrange: Create multiple Document objects and add them to a List. Create an EvaluationRequest instance with this list along with valid userText and responseContent.
  Act: Invoke the toString() method on the EvaluationRequest instance.
  Assert: Verify that the returned string contains the dataList representation with all documents included.
Validation:
  The assertion confirms that multiple documents are properly included in the string output. This is important for debugging scenarios involving multiple context documents.

Scenario 7: Verify toString with special characters in userText

Details:
  TestName: toStringWithSpecialCharactersInUserText
  Description: Verify that the toString method correctly handles special characters (quotes, newlines, etc.) in userText.
Execution:
  Arrange: Create an EvaluationRequest instance with userText containing special characters like single quotes, double quotes, and newline characters.
  Act: Invoke the toString() method on the EvaluationRequest instance.
  Assert: Verify that the returned string properly includes the special characters within the userText field.
Validation:
  The assertion verifies that special characters don't break the string formatting. This is crucial for handling real-world user input that may contain various special characters.

Scenario 8: Verify toString with special characters in responseContent

Details:
  TestName: toStringWithSpecialCharactersInResponseContent
  Description: Verify that the toString method correctly handles special characters in responseContent.
Execution:
  Arrange: Create an EvaluationRequest instance with responseContent containing special characters like quotes, brackets, and escape sequences.
  Act: Invoke the toString() method on the EvaluationRequest instance.
  Assert: Verify that the returned string properly includes the special characters within the chatResponse field.
Validation:
  The assertion confirms that special characters in the response content are handled correctly. This ensures accurate representation of AI-generated responses that may contain various formatting.

Scenario 9: Verify toString output format structure

Details:
  TestName: toStringOutputFormatStructure
  Description: Verify that the toString method returns a string with the exact expected format structure including proper field ordering and delimiters.
Execution:
  Arrange: Create an EvaluationRequest instance with known values for all fields.
  Act: Invoke the toString() method on the EvaluationRequest instance.
  Assert: Verify that the string starts with "EvaluationRequest{", contains fields in order (userText, dataList, chatResponse), and ends with "}".
Validation:
  The assertion verifies the structural integrity of the toString output. Consistent formatting is important for parsing logs and maintaining code readability during debugging.

Scenario 10: Verify toString with all null fields

Details:
  TestName: toStringWithAllNullFields
  Description: Verify that the toString method handles the case where all fields are null without throwing exceptions.
Execution:
  Arrange: Create an EvaluationRequest instance with null for userText, null for dataList, and null for responseContent.
  Act: Invoke the toString() method on the EvaluationRequest instance.
  Assert: Verify that the method returns a valid string representation without throwing NullPointerException.
Validation:
  The assertion confirms that the toString method is robust against null values in all fields. This is a defensive programming check to ensure stability in edge cases.

*/

// ********RoostGPT********

package org.springframework.ai.evaluation;

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.ai.document.Document;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import java.util.Objects;

class EvaluationRequestToStringTest {

	@Test
	@Tag("valid")
	void toStringWithAllFieldsPopulated() {
		// Arrange
		String userText = "What is the capital of France?";
		List<Document> dataList = List.of(new Document("Paris is the capital of France."));
		String responseContent = "The capital of France is Paris.";
		EvaluationRequest request = new EvaluationRequest(userText, dataList, responseContent);
		// Act
		String result = request.toString();
		// Assert
		assertTrue(result.startsWith("EvaluationRequest{"));
		assertTrue(result.contains("userText='" + userText + "'"));
		assertTrue(result.contains("dataList="));
		assertTrue(result.contains("chatResponse=" + responseContent));
		assertTrue(result.endsWith("}"));
	}

	@Test
	@Tag("boundary")
	void toStringWithEmptyUserText() {
		// Arrange
		List<Document> dataList = List.of(new Document("Some document content"));
		String responseContent = "Some response";
		EvaluationRequest request = new EvaluationRequest(dataList, responseContent);
		// Act
		String result = request.toString();
		// Assert
		assertTrue(result.contains("userText=''"));
		assertNotNull(result);
	}

	@Test
	@Tag("boundary")
	void toStringWithEmptyDataList() {
		// Arrange
		String userText = "Test question";
		String responseContent = "Test response";
		EvaluationRequest request = new EvaluationRequest(userText, responseContent);
		// Act
		String result = request.toString();
		// Assert
		assertTrue(result.contains("dataList=[]"));
		assertNotNull(result);
	}

	@Test
	@Tag("boundary")
	void toStringWithNullResponseContent() {
		// Arrange
		String userText = "Test question";
		List<Document> dataList = List.of(new Document("Document content"));
		EvaluationRequest request = new EvaluationRequest(userText, dataList, null);
		// Act
		String result = request.toString();
		// Assert
		assertTrue(result.contains("chatResponse=null"));
		assertNotNull(result);
	}

	@Test
	@Tag("boundary")
	void toStringWithNullUserText() {
		// Arrange
		List<Document> dataList = List.of(new Document("Document content"));
		String responseContent = "Test response";
		EvaluationRequest request = new EvaluationRequest(null, dataList, responseContent);
		// Act
		String result = request.toString();
		// Assert
		assertTrue(result.contains("userText='null'"));
		assertNotNull(result);
	}

	@Test
	@Tag("valid")
	void toStringWithMultipleDocumentsInDataList() {
		// Arrange
		String userText = "Test question";
		List<Document> dataList = new ArrayList<>();
		dataList.add(new Document("First document"));
		dataList.add(new Document("Second document"));
		dataList.add(new Document("Third document"));
		String responseContent = "Test response";
		EvaluationRequest request = new EvaluationRequest(userText, dataList, responseContent);
		// Act
		String result = request.toString();
		// Assert
		assertTrue(result.contains("dataList="));
		assertNotNull(result);
		assertTrue(result.startsWith("EvaluationRequest{"));
		assertTrue(result.endsWith("}"));
	}

	@Test
	@Tag("valid")
	void toStringWithSpecialCharactersInUserText() {
		// Arrange
		String userText = "What's the \"answer\"?\nNew line here";
		List<Document> dataList = Collections.emptyList();
		String responseContent = "Test response";
		EvaluationRequest request = new EvaluationRequest(userText, dataList, responseContent);
		// Act
		String result = request.toString();
		// Assert
		assertTrue(result.contains(userText));
		assertNotNull(result);
	}

	@Test
	@Tag("valid")
	void toStringWithSpecialCharactersInResponseContent() {
		// Arrange
		String userText = "Test question";
		List<Document> dataList = Collections.emptyList();
		String responseContent = "Response with 'quotes' and \"double quotes\" and {brackets}";
		EvaluationRequest request = new EvaluationRequest(userText, dataList, responseContent);
		// Act
		String result = request.toString();
		// Assert
		assertTrue(result.contains(responseContent));
		assertNotNull(result);
	}

	@Test
	@Tag("valid")
	void toStringOutputFormatStructure() {
		// Arrange
		String userText = "Known user text";
		List<Document> dataList = List.of(new Document("Known document"));
		String responseContent = "Known response";
		EvaluationRequest request = new EvaluationRequest(userText, dataList, responseContent);
		// Act
		String result = request.toString();
		// Assert
		assertTrue(result.startsWith("EvaluationRequest{"));
		assertTrue(result.endsWith("}"));

		int userTextIndex = result.indexOf("userText=");
		int dataListIndex = result.indexOf("dataList=");
		int chatResponseIndex = result.indexOf("chatResponse=");

		assertTrue(userTextIndex < dataListIndex);
		assertTrue(dataListIndex < chatResponseIndex);
	}

	@Test
	@Tag("boundary")
	void toStringWithAllNullFields() {
		// Arrange
		EvaluationRequest request = new EvaluationRequest(null, null, null);
		// Act & Assert
		assertDoesNotThrow(() -> {
			String result = request.toString();
			assertNotNull(result);
			assertTrue(result.startsWith("EvaluationRequest{"));
			assertTrue(result.endsWith("}"));
		});
	}

}