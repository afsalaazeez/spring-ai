/*
 * Copyright 2023-2024 the original author or authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test spring-ai-test-timeout-pr using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=equals_40a2ce4724
ROOST_METHOD_SIG_HASH=equals_f2d574000d

Scenario 1: Same Object Reference Returns True

Details:
  TestName: equalsWithSameObjectReference
  Description: Verify that the equals method returns true when comparing an object with itself (same reference).
Execution:
  Arrange: Create an EvaluationResponse object with specific values for pass, score, feedback, and metadata.
  Act: Call the equals method on the object, passing itself as the parameter.
  Assert: Assert that the result is true.
Validation:
  The assertion verifies that the reflexive property of equals is satisfied - an object must always be equal to itself. This is a fundamental contract requirement for the equals method in Java.

---

Scenario 2: Null Object Returns False

Details:
  TestName: equalsWithNullObject
  Description: Verify that the equals method returns false when comparing with a null reference.
Execution:
  Arrange: Create an EvaluationResponse object with valid values.
  Act: Call the equals method on the object, passing null as the parameter.
  Assert: Assert that the result is false.
Validation:
  The assertion verifies that the equals method properly handles null comparisons without throwing a NullPointerException. This is a required behavior per the equals contract.

---

Scenario 3: Different Class Type Returns False

Details:
  TestName: equalsWithDifferentClassType
  Description: Verify that the equals method returns false when comparing with an object of a different class type.
Execution:
  Arrange: Create an EvaluationResponse object and a String object.
  Act: Call the equals method on the EvaluationResponse object, passing the String object as the parameter.
  Assert: Assert that the result is false.
Validation:
  The assertion verifies that the instanceof check correctly identifies objects of different types and returns false. This ensures type safety in equality comparisons.

---

Scenario 4: Equal Objects With All Fields Matching

Details:
  TestName: equalsWithIdenticalFieldValues
  Description: Verify that two EvaluationResponse objects with identical field values are considered equal.
Execution:
  Arrange: Create two EvaluationResponse objects with the same pass (true), score (0.85f), feedback ("Good"), and metadata (Map containing same key-value pairs).
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is true.
Validation:
  The assertion verifies that two distinct objects with identical field values are considered equal. This tests the symmetric property of equals and proper field comparison logic.

---

Scenario 5: Different Pass Value Returns False

Details:
  TestName: equalsWithDifferentPassValue
  Description: Verify that two EvaluationResponse objects with different pass values are not equal.
Execution:
  Arrange: Create two EvaluationResponse objects with different pass values (true vs false), but identical score, feedback, and metadata.
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is false.
Validation:
  The assertion verifies that the pass field is correctly compared in the equals method. Different pass values should result in inequality.

---

Scenario 6: Different Score Value Returns False

Details:
  TestName: equalsWithDifferentScoreValue
  Description: Verify that two EvaluationResponse objects with different score values are not equal.
Execution:
  Arrange: Create two EvaluationResponse objects with the same pass, feedback, and metadata, but different score values (0.5f vs 0.8f).
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is false.
Validation:
  The assertion verifies that the score field is correctly compared using Float.compare. Different score values should result in inequality.

---

Scenario 7: Different Feedback Value Returns False

Details:
  TestName: equalsWithDifferentFeedbackValue
  Description: Verify that two EvaluationResponse objects with different feedback strings are not equal.
Execution:
  Arrange: Create two EvaluationResponse objects with the same pass, score, and metadata, but different feedback values ("Good" vs "Bad").
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is false.
Validation:
  The assertion verifies that the feedback field is correctly compared using Objects.equals. Different feedback strings should result in inequality.

---

Scenario 8: Different Metadata Value Returns False

Details:
  TestName: equalsWithDifferentMetadataValue
  Description: Verify that two EvaluationResponse objects with different metadata maps are not equal.
Execution:
  Arrange: Create two EvaluationResponse objects with the same pass, score, and feedback, but different metadata maps (different keys or values).
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is false.
Validation:
  The assertion verifies that the metadata field is correctly compared using Objects.equals. Different metadata maps should result in inequality.

---

Scenario 9: Both Feedback Values Null Returns True

Details:
  TestName: equalsWithBothFeedbackNull
  Description: Verify that two EvaluationResponse objects with null feedback values are considered equal when other fields match.
Execution:
  Arrange: Create two EvaluationResponse objects with the same pass, score, and metadata, but both with null feedback.
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is true.
Validation:
  The assertion verifies that Objects.equals correctly handles null feedback values. Two null feedback values should be considered equal.

---

Scenario 10: One Feedback Null Other Not Null Returns False

Details:
  TestName: equalsWithOneFeedbackNullOtherNotNull
  Description: Verify that two EvaluationResponse objects where one has null feedback and the other has non-null feedback are not equal.
Execution:
  Arrange: Create two EvaluationResponse objects with the same pass, score, and metadata, but one with null feedback and one with "Good" feedback.
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is false.
Validation:
  The assertion verifies that Objects.equals correctly handles the case where one feedback is null and the other is not. They should not be considered equal.

---

Scenario 11: Both Metadata Values Null Returns True

Details:
  TestName: equalsWithBothMetadataNull
  Description: Verify that two EvaluationResponse objects with null metadata values are considered equal when other fields match.
Execution:
  Arrange: Create two EvaluationResponse objects with the same pass, score, and feedback, but both with null metadata.
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is true.
Validation:
  The assertion verifies that Objects.equals correctly handles null metadata values. Two null metadata values should be considered equal.

---

Scenario 12: One Metadata Null Other Not Null Returns False

Details:
  TestName: equalsWithOneMetadataNullOtherNotNull
  Description: Verify that two EvaluationResponse objects where one has null metadata and the other has non-null metadata are not equal.
Execution:
  Arrange: Create two EvaluationResponse objects with the same pass, score, and feedback, but one with null metadata and one with a populated Map.
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is false.
Validation:
  The assertion verifies that Objects.equals correctly handles the case where one metadata is null and the other is not. They should not be considered equal.

---

Scenario 13: Score Comparison With Float Special Values NaN

Details:
  TestName: equalsWithBothScoresNaN
  Description: Verify that two EvaluationResponse objects with NaN score values are handled correctly by Float.compare.
Execution:
  Arrange: Create two EvaluationResponse objects with the same pass, feedback, and metadata, but both with Float.NaN as score.
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is true.
Validation:
  The assertion verifies that Float.compare correctly handles NaN values. According to Float.compare, NaN is considered equal to itself, which differs from the == operator behavior.

---

Scenario 14: Score Comparison With Positive And Negative Zero

Details:
  TestName: equalsWithPositiveAndNegativeZeroScore
  Description: Verify that two EvaluationResponse objects with positive zero and negative zero scores are handled correctly.
Execution:
  Arrange: Create two EvaluationResponse objects with the same pass, feedback, and metadata, but one with 0.0f and one with -0.0f as score.
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is false.
Validation:
  The assertion verifies that Float.compare distinguishes between positive zero and negative zero. According to Float.compare, 0.0f and -0.0f are not equal.

---

Scenario 15: Symmetric Equality Check

Details:
  TestName: equalsSymmetricProperty
  Description: Verify that the equals method satisfies the symmetric property - if a.equals(b) then b.equals(a).
Execution:
  Arrange: Create two EvaluationResponse objects with identical field values.
  Act: Call equals on the first object with the second, and then call equals on the second object with the first.
  Assert: Assert that both calls return the same result (both true).
Validation:
  The assertion verifies that the symmetric property of the equals contract is satisfied. This is essential for correct behavior in collections and other data structures.

---

Scenario 16: Empty Metadata Maps Are Equal

Details:
  TestName: equalsWithBothEmptyMetadataMaps
  Description: Verify that two EvaluationResponse objects with empty metadata maps are considered equal when other fields match.
Execution:
  Arrange: Create two EvaluationResponse objects with the same pass, score, and feedback, and both with empty Map instances for metadata.
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is true.
Validation:
  The assertion verifies that empty maps are correctly compared as equal. This tests the behavior with edge case of empty collections.

---

Scenario 17: Constructor With Three Parameters Creates Object With Zero Score

Details:
  TestName: equalsWithObjectsCreatedByDifferentConstructors
  Description: Verify that an EvaluationResponse created with the three-parameter constructor (which sets score to 0) equals one created with the four-parameter constructor with score 0.
Execution:
  Arrange: Create one EvaluationResponse using the three-parameter constructor and another using the four-parameter constructor with score set to 0.
  Act: Call the equals method on the first object, passing the second object as the parameter.
  Assert: Assert that the result is true.
Validation:
  The assertion verifies that objects created by different constructors are equal when their field values match. This ensures consistency across different object creation paths.

*/

// ********RoostGPT********

package org.springframework.ai.evaluation;

import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;

class EvaluationResponseEqualsTest {

	@Test
	@Tag("valid")
	void equalsWithSameObjectReference() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response = new EvaluationResponse(true, 0.85f, "Good", metadata);
		// Act
		boolean result = response.equals(response);
		// Assert
		assertTrue(result);
	}

	@Test
	@Tag("invalid")
	void equalsWithNullObject() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response = new EvaluationResponse(true, 0.85f, "Good", metadata);
		// Act
		boolean result = response.equals(null);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("invalid")
	void equalsWithDifferentClassType() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response = new EvaluationResponse(true, 0.85f, "Good", metadata);
		String differentType = "Not an EvaluationResponse";
		// Act
		boolean result = response.equals(differentType);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("valid")
	void equalsWithIdenticalFieldValues() {
		// Arrange
		Map<String, Object> metadata1 = new HashMap<>();
		metadata1.put("key", "value");
		Map<String, Object> metadata2 = new HashMap<>();
		metadata2.put("key", "value");
		EvaluationResponse response1 = new EvaluationResponse(true, 0.85f, "Good", metadata1);
		EvaluationResponse response2 = new EvaluationResponse(true, 0.85f, "Good", metadata2);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertTrue(result);
	}

	@Test
	@Tag("invalid")
	void equalsWithDifferentPassValue() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response1 = new EvaluationResponse(true, 0.85f, "Good", metadata);
		EvaluationResponse response2 = new EvaluationResponse(false, 0.85f, "Good", metadata);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("invalid")
	void equalsWithDifferentScoreValue() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response1 = new EvaluationResponse(true, 0.5f, "Good", metadata);
		EvaluationResponse response2 = new EvaluationResponse(true, 0.8f, "Good", metadata);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("invalid")
	void equalsWithDifferentFeedbackValue() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response1 = new EvaluationResponse(true, 0.85f, "Good", metadata);
		EvaluationResponse response2 = new EvaluationResponse(true, 0.85f, "Bad", metadata);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("invalid")
	void equalsWithDifferentMetadataValue() {
		// Arrange
		Map<String, Object> metadata1 = new HashMap<>();
		metadata1.put("key1", "value1");
		Map<String, Object> metadata2 = new HashMap<>();
		metadata2.put("key2", "value2");
		EvaluationResponse response1 = new EvaluationResponse(true, 0.85f, "Good", metadata1);
		EvaluationResponse response2 = new EvaluationResponse(true, 0.85f, "Good", metadata2);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("boundary")
	void equalsWithBothFeedbackNull() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response1 = new EvaluationResponse(true, 0.85f, null, metadata);
		EvaluationResponse response2 = new EvaluationResponse(true, 0.85f, null, metadata);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertTrue(result);
	}

	@Test
	@Tag("boundary")
	void equalsWithOneFeedbackNullOtherNotNull() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response1 = new EvaluationResponse(true, 0.85f, null, metadata);
		EvaluationResponse response2 = new EvaluationResponse(true, 0.85f, "Good", metadata);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("boundary")
	void equalsWithBothMetadataNull() {
		// Arrange
		EvaluationResponse response1 = new EvaluationResponse(true, 0.85f, "Good", null);
		EvaluationResponse response2 = new EvaluationResponse(true, 0.85f, "Good", null);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertTrue(result);
	}

	@Test
	@Tag("boundary")
	void equalsWithOneMetadataNullOtherNotNull() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response1 = new EvaluationResponse(true, 0.85f, "Good", null);
		EvaluationResponse response2 = new EvaluationResponse(true, 0.85f, "Good", metadata);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("boundary")
	void equalsWithBothScoresNaN() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response1 = new EvaluationResponse(true, Float.NaN, "Good", metadata);
		EvaluationResponse response2 = new EvaluationResponse(true, Float.NaN, "Good", metadata);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertTrue(result);
	}

	@Test
	@Tag("boundary")
	void equalsWithPositiveAndNegativeZeroScore() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response1 = new EvaluationResponse(true, 0.0f, "Good", metadata);
		EvaluationResponse response2 = new EvaluationResponse(true, -0.0f, "Good", metadata);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("valid")
	void equalsSymmetricProperty() {
		// Arrange
		Map<String, Object> metadata1 = new HashMap<>();
		metadata1.put("key", "value");
		Map<String, Object> metadata2 = new HashMap<>();
		metadata2.put("key", "value");
		EvaluationResponse response1 = new EvaluationResponse(true, 0.85f, "Good", metadata1);
		EvaluationResponse response2 = new EvaluationResponse(true, 0.85f, "Good", metadata2);
		// Act
		boolean result1 = response1.equals(response2);
		boolean result2 = response2.equals(response1);
		// Assert
		assertTrue(result1);
		assertTrue(result2);
		assertEquals(result1, result2);
	}

	@Test
	@Tag("boundary")
	void equalsWithBothEmptyMetadataMaps() {
		// Arrange
		Map<String, Object> metadata1 = new HashMap<>();
		Map<String, Object> metadata2 = new HashMap<>();
		EvaluationResponse response1 = new EvaluationResponse(true, 0.85f, "Good", metadata1);
		EvaluationResponse response2 = new EvaluationResponse(true, 0.85f, "Good", metadata2);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertTrue(result);
	}

	@Test
	@Tag("valid")
	void equalsWithObjectsCreatedByDifferentConstructors() {
		// Arrange
		Map<String, Object> metadata1 = new HashMap<>();
		metadata1.put("key", "value");
		Map<String, Object> metadata2 = new HashMap<>();
		metadata2.put("key", "value");
		EvaluationResponse response1 = new EvaluationResponse(true, "Good", metadata1);
		EvaluationResponse response2 = new EvaluationResponse(true, 0, "Good", metadata2);
		// Act
		boolean result = response1.equals(response2);
		// Assert
		assertTrue(result);
	}

}