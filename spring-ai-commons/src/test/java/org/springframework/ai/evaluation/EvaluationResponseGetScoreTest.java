/*
 * Copyright 2023-2024 the original author or authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test spring-ai-test-timeout-pr using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=getScore_b1075041fd
ROOST_METHOD_SIG_HASH=getScore_6895b44944

Here are your existing test cases which we found out and are not considered for test generation:

File Path: /var/tmp/Roost/RoostGPT/spring-ai-test-timeout-pr/1e559eba-5752-4feb-88f7-63ae643c0140/source/spring-ai/spring-ai-commons/src/test/java/org/springframework/ai/document/ContentFormatterTests.java
Tests:
    "@Test
void mutateTextDocument() {
    Document original = new Document("id", "original text", Map.of("key", "value"));
    Document mutated = original.mutate().text("modified text").metadata("newKey", "newValue").score(0.9).build();
    assertThat(mutated.getId()).isEqualTo("id");
    assertThat(mutated.getText()).isEqualTo("modified text");
    assertThat(mutated.getMetadata()).containsEntry("newKey", "newValue");
    assertThat(mutated.getScore()).isEqualTo(0.9);

    assertThat(original.getText()).isEqualTo("original text");
    assertThat(original.getScore()).isNull();
}
"
    "@Test
void scoreHandling() {
    Document document = Document.builder().text("test").score(0.85).build();
    assertThat(document.getScore()).isEqualTo(0.85);
    Document documentWithoutScore = new Document("test");
    assertThat(documentWithoutScore.getScore()).isNull();
}
"

File Path: /var/tmp/Roost/RoostGPT/spring-ai-test-timeout-pr/1e559eba-5752-4feb-88f7-63ae643c0140/source/spring-ai/spring-ai-commons/src/test/java/org/springframework/ai/document/DocumentTests.java
Tests:
    "@Test
void testScore() {
    Double score = 0.95;
    Document document = Document.builder().text("Test content").score(score).build();
    assertThat(document.getScore()).isEqualTo(score);
}
"
    "@Test
void testNullScore() {
    Document document = Document.builder().text("Test content").score(null).build();
    assertThat(document.getScore()).isNull();
}
"
    "@Test
void testDocumentBuilderChaining() {
    Map<String, Object> metadata = new HashMap<>();
    metadata.put("chain", "test");
    Document document = Document.builder().text("Chain test").metadata(metadata).metadata("additional", "value").score(0.85).build();
    assertThat(document.getText()).isEqualTo("Chain test");
    assertThat(document.getMetadata()).containsEntry("chain", "test");
    assertThat(document.getMetadata()).containsEntry("additional", "value");
    assertThat(document.getScore()).isEqualTo(0.85);
}
"
    "@Test
void testDocumentWithScoreGreaterThanOne() {
    Document document = Document.builder().text("High score test").score(1.5).build();
    assertThat(document.getScore()).isEqualTo(1.5);
}
"
    "@Test
void testMutateWithChanges() {
    Document original = Document.builder().text("Original text").score(0.5).metadata("original", "value").build();
    Document mutated = original.mutate().text("Mutated text").score(0.8).metadata("new", "metadata").build();
    assertThat(mutated.getText()).isEqualTo("Mutated text");
    assertThat(mutated.getScore()).isEqualTo(0.8);
    assertThat(mutated.getMetadata()).containsEntry("new", "metadata");

    assertThat(original.getText()).isEqualTo("Original text");
}
"

File Path: /var/tmp/Roost/RoostGPT/spring-ai-test-timeout-pr/1e559eba-5752-4feb-88f7-63ae643c0140/source/spring-ai/spring-ai-commons/src/test/java/org/springframework/ai/transformer/splitter/TextSplitterTests.java
Tests:
    "@Test
public void testScorePreservation() {

    Double originalScore = 0.95;
    var doc = Document.builder().text("This is a test document that will be split into multiple chunks.").metadata(Map.of("source", "test.txt")).score(originalScore).build();

    List<Document> chunks = testTextSplitter.apply(List.of(doc));

    assertThat(chunks).hasSize(2);
    assertThat(chunks.get(0).getScore()).isEqualTo(originalScore);
    assertThat(chunks.get(1).getScore()).isEqualTo(originalScore);
}
"
    "@Test
public void testEnhancedMetadataWithMultipleDocuments() {

    var doc1 = Document.builder().text("First document with score and metadata.").metadata(Map.of("type", "article", "priority", "high")).score(0.8).build();
    var doc2 = Document.builder().text("Second document with different score.").metadata(Map.of("type", "report", "priority", "medium")).score(0.6).build();
    String originalId1 = doc1.getId();
    String originalId2 = doc2.getId();

    List<Document> chunks = testTextSplitter.apply(List.of(doc1, doc2));

    assertThat(chunks).hasSize(4);

    for (int i = 0; i < 2; i++) {
        Document chunk = chunks.get(i);
        assertThat(chunk.getScore()).isEqualTo(0.8);
        assertThat(chunk.getMetadata().get("parent_document_id")).isEqualTo(originalId1);
        assertThat(chunk.getMetadata().get("chunk_index")).isEqualTo(i);
        assertThat(chunk.getMetadata().get("total_chunks")).isEqualTo(2);
        assertThat(chunk.getMetadata().get("type")).isEqualTo("article");
        assertThat(chunk.getMetadata().get("priority")).isEqualTo("high");
    }

    for (int i = 2; i < 4; i++) {
        Document chunk = chunks.get(i);
        assertThat(chunk.getScore()).isEqualTo(0.6);
        assertThat(chunk.getMetadata().get("parent_document_id")).isEqualTo(originalId2);
        assertThat(chunk.getMetadata().get("chunk_index")).isEqualTo(i - 2);
        assertThat(chunk.getMetadata().get("total_chunks")).isEqualTo(2);
        assertThat(chunk.getMetadata().get("type")).isEqualTo("report");
        assertThat(chunk.getMetadata().get("priority")).isEqualTo("medium");
    }
}
"

File Path: /var/tmp/Roost/RoostGPT/spring-ai-test-timeout-pr/1e559eba-5752-4feb-88f7-63ae643c0140/source/spring-ai/spring-ai-commons/src/test/java/org/springframework/ai/document/DocumentBuilderTests.java
Tests:
    "@Test
void testBuilderMethodChaining() {
    Document document = this.builder.text("chained").id("chain-id").metadata("key1", "value1").metadata("key2", "value2").score(0.75).build();
    assertThat(document.getText()).isEqualTo("chained");
    assertThat(document.getId()).isEqualTo("chain-id");
    assertThat(document.getMetadata()).hasSize(2);
    assertThat(document.getScore()).isEqualTo(0.75);
}
"Scenario 1: Retrieve Score from EvaluationResponse with Positive Score Value

Details:
  TestName: getScoreReturnsPositiveScoreValue
  Description: Verify that the getScore method correctly returns a positive float score value that was set during object construction using the four-parameter constructor.
Execution:
  Arrange: Create an EvaluationResponse object with a positive score value (e.g., 0.85f), along with pass status, feedback, and metadata.
  Act: Invoke the getScore() method on the created EvaluationResponse instance.
  Assert: Assert that the returned score equals 0.85f.
Validation:
  The assertion verifies that the getScore method accurately retrieves the score value stored in the object. This is essential for evaluation workflows where the score represents the quality or relevance of a response.

Scenario 2: Retrieve Score from EvaluationResponse with Zero Score Value

Details:
  TestName: getScoreReturnsZeroWhenExplicitlySet
  Description: Verify that the getScore method returns zero when the score is explicitly set to 0.0f during construction.
Execution:
  Arrange: Create an EvaluationResponse object with score set to 0.0f using the four-parameter constructor.
  Act: Invoke the getScore() method on the EvaluationResponse instance.
  Assert: Assert that the returned score equals 0.0f.
Validation:
  The assertion confirms that zero scores are handled correctly. A zero score may indicate a completely irrelevant or incorrect evaluation result, and the system must accurately report this value.

Scenario 3: Retrieve Default Score from Three-Parameter Constructor

Details:
  TestName: getScoreReturnsDefaultZeroFromThreeParameterConstructor
  Description: Verify that the getScore method returns the default value of 0 when the EvaluationResponse is created using the three-parameter constructor that does not accept a score.
Execution:
  Arrange: Create an EvaluationResponse object using the constructor with only pass, feedback, and metadata parameters.
  Act: Invoke the getScore() method on the EvaluationResponse instance.
  Assert: Assert that the returned score equals 0.0f.
Validation:
  The assertion verifies that the default score initialization works correctly. This is important for cases where score is not applicable or not computed, ensuring consistent behavior.

Scenario 4: Retrieve Score with Maximum Float Value

Details:
  TestName: getScoreReturnsMaximumFloatValue
  Description: Verify that the getScore method correctly handles and returns the maximum possible float value.
Execution:
  Arrange: Create an EvaluationResponse object with score set to Float.MAX_VALUE.
  Act: Invoke the getScore() method on the EvaluationResponse instance.
  Assert: Assert that the returned score equals Float.MAX_VALUE.
Validation:
  The assertion ensures that extreme boundary values are handled without overflow or precision loss. This tests the robustness of the score field for edge cases.

Scenario 5: Retrieve Score with Minimum Float Value

Details:
  TestName: getScoreReturnsMinimumFloatValue
  Description: Verify that the getScore method correctly handles and returns the minimum positive float value.
Execution:
  Arrange: Create an EvaluationResponse object with score set to Float.MIN_VALUE.
  Act: Invoke the getScore() method on the EvaluationResponse instance.
  Assert: Assert that the returned score equals Float.MIN_VALUE.
Validation:
  The assertion confirms that very small positive values are preserved accurately. This is important for precision-sensitive evaluation scenarios.

Scenario 6: Retrieve Negative Score Value

Details:
  TestName: getScoreReturnsNegativeScoreValue
  Description: Verify that the getScore method correctly returns a negative float score value when set during construction.
Execution:
  Arrange: Create an EvaluationResponse object with a negative score value (e.g., -0.5f).
  Act: Invoke the getScore() method on the EvaluationResponse instance.
  Assert: Assert that the returned score equals -0.5f.
Validation:
  The assertion verifies that negative scores are supported and returned correctly. Negative scores might represent penalties or below-baseline evaluations in certain use cases.

Scenario 7: Retrieve Score with Fractional Precision

Details:
  TestName: getScoreReturnsFractionalPrecisionValue
  Description: Verify that the getScore method maintains fractional precision for scores with multiple decimal places.
Execution:
  Arrange: Create an EvaluationResponse object with score set to 0.123456f.
  Act: Invoke the getScore() method on the EvaluationResponse instance.
  Assert: Assert that the returned score equals 0.123456f.
Validation:
  The assertion ensures that floating-point precision is maintained. This is critical for evaluation systems that require fine-grained scoring distinctions.

Scenario 8: Retrieve Score Value of Exactly One

Details:
  TestName: getScoreReturnsExactlyOne
  Description: Verify that the getScore method correctly returns a score of exactly 1.0f, which often represents a perfect or maximum normalized score.
Execution:
  Arrange: Create an EvaluationResponse object with score set to 1.0f.
  Act: Invoke the getScore() method on the EvaluationResponse instance.
  Assert: Assert that the returned score equals 1.0f.
Validation:
  The assertion confirms that the common boundary value of 1.0 is handled correctly. In many evaluation systems, 1.0 represents a perfect score.

Scenario 9: Retrieve Score Greater Than One

Details:
  TestName: getScoreReturnsValueGreaterThanOne
  Description: Verify that the getScore method correctly returns scores greater than 1.0f, which may be valid in non-normalized scoring systems.
Execution:
  Arrange: Create an EvaluationResponse object with score set to 2.5f.
  Act: Invoke the getScore() method on the EvaluationResponse instance.
  Assert: Assert that the returned score equals 2.5f.
Validation:
  The assertion verifies that the method does not impose artificial constraints on score values. Some evaluation systems may use unbounded scoring ranges.

Scenario 10: Verify Score Consistency Across Multiple Calls

Details:
  TestName: getScoreReturnsConsistentValueAcrossMultipleCalls
  Description: Verify that calling getScore multiple times on the same EvaluationResponse instance returns the same value consistently.
Execution:
  Arrange: Create an EvaluationResponse object with a specific score value (e.g., 0.75f).
  Act: Invoke the getScore() method multiple times on the same instance.
  Assert: Assert that all returned values are equal to 0.75f.
Validation:
  The assertion confirms immutability and consistency of the score field. Since the field is final, repeated calls should always return the same value.

Scenario 11: Retrieve Score with Pass Status True

Details:
  TestName: getScoreWithPassStatusTrue
  Description: Verify that the getScore method returns the correct score value regardless of the pass status being true.
Execution:
  Arrange: Create an EvaluationResponse object with pass set to true and score set to 0.9f.
  Act: Invoke the getScore() method on the EvaluationResponse instance.
  Assert: Assert that the returned score equals 0.9f.
Validation:
  The assertion verifies that the score retrieval is independent of the pass status. The score and pass fields should operate independently.

Scenario 12: Retrieve Score with Pass Status False

Details:
  TestName: getScoreWithPassStatusFalse
  Description: Verify that the getScore method returns the correct score value regardless of the pass status being false.
Execution:
  Arrange: Create an EvaluationResponse object with pass set to false and score set to 0.3f.
  Act: Invoke the getScore() method on the EvaluationResponse instance.
  Assert: Assert that the returned score equals 0.3f.
Validation:
  The assertion confirms that a failing evaluation can still have an associated score. This supports scenarios where partial credit or gradual scoring is needed.

*/

// ********RoostGPT********

package org.springframework.ai.evaluation;

import java.util.Map;
import java.util.HashMap;
import java.util.Objects;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.DisplayName;
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.*;

class EvaluationResponseGetScoreTest {

	@Test
	@Tag("valid")
	@DisplayName("Retrieve Score from EvaluationResponse with Positive Score Value")
	void getScoreReturnsPositiveScoreValue() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key", "value");
		EvaluationResponse response = new EvaluationResponse(true, 0.85f, "Good feedback", metadata);
		// Act
		float actualScore = response.getScore();
		// Assert
		assertEquals(0.85f, actualScore);
	}

	@Test
	@Tag("boundary")
	@DisplayName("Retrieve Score from EvaluationResponse with Zero Score Value")
	void getScoreReturnsZeroWhenExplicitlySet() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(false, 0.0f, "Zero score feedback", metadata);
		// Act
		float actualScore = response.getScore();
		// Assert
		assertEquals(0.0f, actualScore);
	}

	@Test
	@Tag("valid")
	@DisplayName("Retrieve Default Score from Three-Parameter Constructor")
	void getScoreReturnsDefaultZeroFromThreeParameterConstructor() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("source", "test");
		EvaluationResponse response = new EvaluationResponse(true, "Feedback without score", metadata);
		// Act
		float actualScore = response.getScore();
		// Assert
		assertEquals(0.0f, actualScore);
	}

	@Test
	@Tag("boundary")
	@DisplayName("Retrieve Score with Maximum Float Value")
	void getScoreReturnsMaximumFloatValue() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(true, Float.MAX_VALUE, "Max score feedback", metadata);
		// Act
		float actualScore = response.getScore();
		// Assert
		assertEquals(Float.MAX_VALUE, actualScore);
	}

	@Test
	@Tag("boundary")
	@DisplayName("Retrieve Score with Minimum Float Value")
	void getScoreReturnsMinimumFloatValue() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(true, Float.MIN_VALUE, "Min score feedback", metadata);
		// Act
		float actualScore = response.getScore();
		// Assert
		assertEquals(Float.MIN_VALUE, actualScore);
	}

	@Test
	@Tag("valid")
	@DisplayName("Retrieve Negative Score Value")
	void getScoreReturnsNegativeScoreValue() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(false, -0.5f, "Negative score feedback", metadata);
		// Act
		float actualScore = response.getScore();
		// Assert
		assertEquals(-0.5f, actualScore);
	}

	@Test
	@Tag("valid")
	@DisplayName("Retrieve Score with Fractional Precision")
	void getScoreReturnsFractionalPrecisionValue() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(true, 0.123456f, "Precision feedback", metadata);
		// Act
		float actualScore = response.getScore();
		// Assert
		assertEquals(0.123456f, actualScore);
	}

	@Test
	@Tag("boundary")
	@DisplayName("Retrieve Score Value of Exactly One")
	void getScoreReturnsExactlyOne() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(true, 1.0f, "Perfect score feedback", metadata);
		// Act
		float actualScore = response.getScore();
		// Assert
		assertEquals(1.0f, actualScore);
	}

	@Test
	@Tag("valid")
	@DisplayName("Retrieve Score Greater Than One")
	void getScoreReturnsValueGreaterThanOne() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(true, 2.5f, "Above one score feedback", metadata);
		// Act
		float actualScore = response.getScore();
		// Assert
		assertEquals(2.5f, actualScore);
	}

	@Test
	@Tag("valid")
	@DisplayName("Verify Score Consistency Across Multiple Calls")
	void getScoreReturnsConsistentValueAcrossMultipleCalls() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(true, 0.75f, "Consistent feedback", metadata);
		// Act
		float firstCall = response.getScore();
		float secondCall = response.getScore();
		float thirdCall = response.getScore();
		// Assert
		assertEquals(0.75f, firstCall);
		assertEquals(0.75f, secondCall);
		assertEquals(0.75f, thirdCall);
		assertEquals(firstCall, secondCall);
		assertEquals(secondCall, thirdCall);
	}

	@Test
	@Tag("valid")
	@DisplayName("Retrieve Score with Pass Status True")
	void getScoreWithPassStatusTrue() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("status", "passed");
		EvaluationResponse response = new EvaluationResponse(true, 0.9f, "Pass feedback", metadata);
		// Act
		float actualScore = response.getScore();
		// Assert
		assertEquals(0.9f, actualScore);
	}

	@Test
	@Tag("valid")
	@DisplayName("Retrieve Score with Pass Status False")
	void getScoreWithPassStatusFalse() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("status", "failed");
		EvaluationResponse response = new EvaluationResponse(false, 0.3f, "Fail feedback", metadata);
		// Act
		float actualScore = response.getScore();
		// Assert
		assertEquals(0.3f, actualScore);
	}

}