/*
 * Copyright 2023-2024 the original author or authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test spring-ai-test-timeout-pr using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=toString_cd6cc9c60c
ROOST_METHOD_SIG_HASH=toString_ceffa8036e

Here are your existing test cases which we found out and are not considered for test generation:

File Path: /var/tmp/Roost/RoostGPT/spring-ai-test-timeout-pr/1e559eba-5752-4feb-88f7-63ae643c0140/source/spring-ai/spring-ai-commons/src/test/java/org/springframework/ai/document/id/JdkSha256HexIdGeneratorTest.java
Tests:
    "@Test
void messageDigestReturnsDistinctInstances() {
    final MessageDigest md1 = this.testee.getMessageDigest();
    final MessageDigest md2 = this.testee.getMessageDigest();
    Assertions.assertThat(md1 != md2).isTrue();
    Assertions.assertThat(md1.getAlgorithm()).isEqualTo(md2.getAlgorithm());
    Assertions.assertThat(md1.getDigestLength()).isEqualTo(md2.getDigestLength());
    Assertions.assertThat(md1.getProvider()).isEqualTo(md2.getProvider());
    Assertions.assertThat(md1.toString()).isEqualTo(md2.toString());
}
"

File Path: /var/tmp/Roost/RoostGPT/spring-ai-test-timeout-pr/1e559eba-5752-4feb-88f7-63ae643c0140/source/spring-ai/spring-ai-commons/src/test/java/org/springframework/ai/document/DocumentTests.java
Tests:
    "@Test
void testToString() {
    Media media = getMedia();
    Map<String, Object> metadata = new HashMap<>();
    metadata.put("key", "value");
    Double score = 0.95;
    Document document = Document.builder().id("customId").text("Test content").media(null).metadata(metadata).score(score).build();
    String toString = document.toString();
    assertThat(toString).contains("id='customId'").contains("text='Test content'").contains("metadata=" + metadata).contains("score=" + score);
}
"

File Path: /var/tmp/Roost/RoostGPT/spring-ai-test-timeout-pr/1e559eba-5752-4feb-88f7-63ae643c0140/source/spring-ai/spring-ai-commons/src/test/java/org/springframework/ai/writer/FileDocumentWriterTest.java
Tests:
    "@Test
void setUp() {
    this.testFileName = this.tempDir.resolve("file-document-test-output.txt").toString();
    this.testDocuments = List.of(Document.builder().text("Document one introduces the core functionality of Spring AI.").metadata("page_number", "1").metadata("end_page_number", "2").metadata("source", "intro.pdf").metadata("title", "Spring AI Overview").metadata("author", "QA Team").build(), Document.builder().text("Document two illustrates multi-line handling and line breaks.\nEnsure preservation of formatting.").metadata("page_number", "3").metadata("end_page_number", "4").metadata("source", "formatting.pdf").build(), Document.builder().text("Document three checks metadata inclusion and output formatting behavior.").metadata("page_number", "5").metadata("end_page_number", "6").metadata("version", "v1.2").build());
}
"
    "@Test
void testMetadataModeAllWithDocumentMarkers() throws IOException {
    var writer = new FileDocumentWriter(this.testFileName, true, MetadataMode.ALL, false);
    writer.accept(this.testDocuments);
    List<String> lines = Files.readAllLines(Path.of(this.testFileName));
    assertEquals("", lines.get(0));
    assertEquals("### Doc: 0, pages:[1,2]", lines.get(1));
    String subListToString = lines.subList(2, 7).toString();
    assertTrue(subListToString.contains("page_number: 1"));
    assertTrue(subListToString.contains("end_page_number: 2"));
    assertTrue(subListToString.contains("source: intro.pdf"));
    assertTrue(subListToString.contains("title: Spring AI Overview"));
    assertTrue(subListToString.contains("author: QA Team"));
    assertEquals("", lines.get(7));
    assertEquals("Document one introduces the core functionality of Spring AI.", lines.get(8));
    assertEquals("### Doc: 1, pages:[3,4]", lines.get(9));
    subListToString = lines.subList(10, 13).toString();
    assertTrue(subListToString.contains("page_number: 3"));
    assertTrue(subListToString.contains("source: formatting.pdf"));
    assertTrue(subListToString.contains("end_page_number: 4"));
    assertEquals("", lines.get(13));
    assertEquals("Document two illustrates multi-line handling and line breaks.", lines.get(14));
    assertEquals("Ensure preservation of formatting.", lines.get(15));
    assertEquals("### Doc: 2, pages:[5,6]", lines.get(16));
    subListToString = lines.subList(17, 20).toString();
    assertTrue(subListToString.contains("page_number: 5"));
    assertTrue(subListToString.contains("end_page_number: 6"));
    assertTrue(subListToString.contains("version: v1.2"));
    assertEquals("", lines.get(20));
    assertEquals("Document three checks metadata inclusion and output formatting behavior.", lines.get(21));
}
"Scenario 1: Verify toString with pass=true, positive score, non-null feedback, and non-empty metadata

Details:
  TestName: toStringWithPassTruePositiveScoreAndMetadata
  Description: This test verifies that the toString method correctly formats all fields when pass is true, score is a positive float value, feedback contains a string, and metadata contains key-value pairs.
Execution:
  Arrange: Create a Map with sample metadata entries. Instantiate an EvaluationResponse with pass=true, score=0.85f, feedback="Good evaluation", and the metadata map.
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string contains "pass=true", "score=0.85", "feedback='Good evaluation'", and the metadata map representation.
Validation:
  The assertion verifies that all fields are properly concatenated in the expected format. This ensures the toString method provides a complete and accurate string representation for debugging and logging purposes.

Scenario 2: Verify toString with pass=false and zero score

Details:
  TestName: toStringWithPassFalseAndZeroScore
  Description: This test checks that the toString method correctly represents an EvaluationResponse when pass is false and score is zero.
Execution:
  Arrange: Create an EvaluationResponse using the constructor that sets score to 0 by default (pass=false, feedback="Failed", metadata=empty map).
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string contains "pass=false" and "score=0.0".
Validation:
  The assertion confirms that boolean false and zero score values are correctly represented in the string output, which is important for accurate state representation.

Scenario 3: Verify toString with null feedback

Details:
  TestName: toStringWithNullFeedback
  Description: This test verifies that the toString method handles null feedback gracefully without throwing an exception.
Execution:
  Arrange: Create an EvaluationResponse with pass=true, score=0.5f, feedback=null, and an empty metadata map.
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string contains "feedback='null'" and does not throw a NullPointerException.
Validation:
  The assertion ensures that null values for feedback are handled properly in the string representation, preventing runtime errors during logging or debugging.

Scenario 4: Verify toString with empty metadata map

Details:
  TestName: toStringWithEmptyMetadata
  Description: This test checks that the toString method correctly represents an empty metadata map.
Execution:
  Arrange: Create an EvaluationResponse with pass=true, score=1.0f, feedback="Perfect", and an empty HashMap for metadata.
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string contains "metadata={}" representing an empty map.
Validation:
  The assertion confirms that empty metadata is properly displayed, which is important for distinguishing between empty and null metadata states.

Scenario 5: Verify toString with null metadata

Details:
  TestName: toStringWithNullMetadata
  Description: This test verifies that the toString method handles null metadata without throwing an exception.
Execution:
  Arrange: Create an EvaluationResponse with pass=false, score=0.0f, feedback="No data", and metadata=null.
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string contains "metadata=null" and completes without exception.
Validation:
  The assertion ensures null metadata is safely represented in the output string, preventing NullPointerException during string concatenation.

Scenario 6: Verify toString with negative score

Details:
  TestName: toStringWithNegativeScore
  Description: This test checks that the toString method correctly represents a negative score value.
Execution:
  Arrange: Create an EvaluationResponse with pass=false, score=-0.5f, feedback="Below threshold", and a non-empty metadata map.
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string contains "score=-0.5".
Validation:
  The assertion confirms that negative float values are correctly displayed, which may be relevant for certain evaluation scoring systems.

Scenario 7: Verify toString with maximum float score

Details:
  TestName: toStringWithMaxFloatScore
  Description: This test verifies that the toString method correctly handles the maximum float value for score.
Execution:
  Arrange: Create an EvaluationResponse with pass=true, score=Float.MAX_VALUE, feedback="Maximum score", and an empty metadata map.
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string contains the string representation of Float.MAX_VALUE.
Validation:
  The assertion ensures that extreme float values are properly converted to string format without overflow or formatting issues.

Scenario 8: Verify toString with special characters in feedback

Details:
  TestName: toStringWithSpecialCharactersInFeedback
  Description: This test checks that the toString method correctly handles special characters including quotes and newlines in the feedback string.
Execution:
  Arrange: Create an EvaluationResponse with pass=true, score=0.75f, feedback="Test's \"feedback\"\nwith newline", and an empty metadata map.
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string contains the feedback with special characters preserved.
Validation:
  The assertion confirms that special characters in feedback are not escaped or modified, maintaining the original content integrity.

Scenario 9: Verify toString with metadata containing multiple entries

Details:
  TestName: toStringWithMultipleMetadataEntries
  Description: This test verifies that the toString method correctly represents metadata with multiple key-value pairs.
Execution:
  Arrange: Create a Map with multiple entries (e.g., "key1"="value1", "key2"=123, "key3"=true). Create an EvaluationResponse with this metadata.
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string contains the metadata map representation with all entries.
Validation:
  The assertion ensures that complex metadata structures are properly represented in the string output for comprehensive debugging information.

Scenario 10: Verify toString with empty feedback string

Details:
  TestName: toStringWithEmptyFeedbackString
  Description: This test checks that the toString method correctly handles an empty string for feedback.
Execution:
  Arrange: Create an EvaluationResponse with pass=true, score=0.5f, feedback="", and an empty metadata map.
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string contains "feedback=''" representing an empty string.
Validation:
  The assertion confirms that empty strings are distinguished from null values in the output, providing accurate state representation.

Scenario 11: Verify toString format structure

Details:
  TestName: toStringReturnsCorrectFormatStructure
  Description: This test verifies that the toString method returns a string with the exact expected format structure including class name and field delimiters.
Execution:
  Arrange: Create an EvaluationResponse with known values for all fields.
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string starts with "EvaluationResponse{" and ends with "}", and contains proper comma separators between fields.
Validation:
  The assertion ensures the string follows the standard Java toString convention, making it consistent with other classes and easy to parse if needed.

Scenario 12: Verify toString with metadata containing nested objects

Details:
  TestName: toStringWithNestedObjectsInMetadata
  Description: This test checks that the toString method handles metadata containing nested Map or List objects.
Execution:
  Arrange: Create a Map with a nested Map as one of its values. Create an EvaluationResponse with this complex metadata.
  Act: Call the toString() method on the EvaluationResponse instance.
  Assert: Verify that the returned string contains the nested structure representation from the metadata map's toString.
Validation:
  The assertion confirms that complex nested metadata structures are represented through their own toString methods, providing complete information.

*/

// ********RoostGPT********

package org.springframework.ai.evaluation;

import java.util.HashMap;
import java.util.List;
import java.util.Map;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import java.util.Objects;

class EvaluationResponseToStringTest {

	@Test
	@Tag("valid")
	void toStringWithPassTruePositiveScoreAndMetadata() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key1", "value1");
		metadata.put("key2", 42);
		EvaluationResponse response = new EvaluationResponse(true, 0.85f, "Good evaluation", metadata);
		// Act
		String result = response.toString();
		// Assert
		assertTrue(result.contains("pass=true"));
		assertTrue(result.contains("score=0.85"));
		assertTrue(result.contains("feedback='Good evaluation'"));
		assertTrue(result.contains("metadata="));
		assertTrue(result.contains("key1"));
		assertTrue(result.contains("value1"));
	}

	@Test
	@Tag("valid")
	void toStringWithPassFalseAndZeroScore() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(false, "Failed", metadata);
		// Act
		String result = response.toString();
		// Assert
		assertTrue(result.contains("pass=false"));
		assertTrue(result.contains("score=0.0"));
		assertTrue(result.contains("feedback='Failed'"));
	}

	@Test
	@Tag("boundary")
	void toStringWithNullFeedback() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(true, 0.5f, null, metadata);
		// Act
		String result = response.toString();
		// Assert
		assertNotNull(result);
		assertTrue(result.contains("feedback='null'"));
		assertTrue(result.contains("pass=true"));
		assertTrue(result.contains("score=0.5"));
	}

	@Test
	@Tag("valid")
	void toStringWithEmptyMetadata() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(true, 1.0f, "Perfect", metadata);
		// Act
		String result = response.toString();
		// Assert
		assertTrue(result.contains("metadata={}"));
		assertTrue(result.contains("feedback='Perfect'"));
		assertTrue(result.contains("score=1.0"));
	}

	@Test
	@Tag("boundary")
	void toStringWithNullMetadata() {
		// Arrange
		EvaluationResponse response = new EvaluationResponse(false, 0.0f, "No data", null);
		// Act
		String result = response.toString();
		// Assert
		assertNotNull(result);
		assertTrue(result.contains("metadata=null"));
		assertTrue(result.contains("pass=false"));
		assertTrue(result.contains("feedback='No data'"));
	}

	@Test
	@Tag("boundary")
	void toStringWithNegativeScore() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("reason", "below threshold");
		EvaluationResponse response = new EvaluationResponse(false, -0.5f, "Below threshold", metadata);
		// Act
		String result = response.toString();
		// Assert
		assertTrue(result.contains("score=-0.5"));
		assertTrue(result.contains("pass=false"));
	}

	@Test
	@Tag("boundary")
	void toStringWithMaxFloatScore() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(true, Float.MAX_VALUE, "Maximum score", metadata);
		// Act
		String result = response.toString();
		// Assert
		assertNotNull(result);
		assertTrue(result.contains("score=" + Float.MAX_VALUE));
		assertTrue(result.contains("pass=true"));
	}

	@Test
	@Tag("valid")
	void toStringWithSpecialCharactersInFeedback() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		String specialFeedback = "Test's \"feedback\"\nwith newline";
		EvaluationResponse response = new EvaluationResponse(true, 0.75f, specialFeedback, metadata);
		// Act
		String result = response.toString();
		// Assert
		assertNotNull(result);
		assertTrue(result.contains("feedback='" + specialFeedback + "'"));
	}

	@Test
	@Tag("valid")
	void toStringWithMultipleMetadataEntries() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("key1", "value1");
		metadata.put("key2", 123);
		metadata.put("key3", true);
		EvaluationResponse response = new EvaluationResponse(true, 0.9f, "Multiple metadata", metadata);
		// Act
		String result = response.toString();
		// Assert
		assertTrue(result.contains("metadata="));
		assertTrue(result.contains("key1"));
		assertTrue(result.contains("key2"));
		assertTrue(result.contains("key3"));
	}

	@Test
	@Tag("boundary")
	void toStringWithEmptyFeedbackString() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		EvaluationResponse response = new EvaluationResponse(true, 0.5f, "", metadata);
		// Act
		String result = response.toString();
		// Assert
		assertTrue(result.contains("feedback=''"));
	}

	@Test
	@Tag("valid")
	void toStringReturnsCorrectFormatStructure() {
		// Arrange
		Map<String, Object> metadata = new HashMap<>();
		metadata.put("testKey", "testValue");
		EvaluationResponse response = new EvaluationResponse(true, 0.8f, "Test feedback", metadata);
		// Act
		String result = response.toString();
		// Assert
		assertTrue(result.startsWith("EvaluationResponse{"));
		assertTrue(result.endsWith("}"));
		assertTrue(result.contains(", score="));
		assertTrue(result.contains(", feedback="));
		assertTrue(result.contains(", metadata="));
	}

	@Test
	@Tag("valid")
	void toStringWithNestedObjectsInMetadata() {
		// Arrange
		Map<String, Object> nestedMap = new HashMap<>();
		nestedMap.put("nestedKey", "nestedValue");

		Map<String, Object> metadata = new HashMap<>();
		metadata.put("outerKey", nestedMap);
		metadata.put("listKey", List.of("item1", "item2"));

		EvaluationResponse response = new EvaluationResponse(true, 0.95f, "Nested metadata", metadata);
		// Act
		String result = response.toString();
		// Assert
		assertNotNull(result);
		assertTrue(result.contains("metadata="));
		assertTrue(result.contains("outerKey"));
		assertTrue(result.contains("nestedKey"));
	}

}