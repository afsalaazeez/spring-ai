/*
 * Copyright 2023-2024 the original author or authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test zbio-dec-12-sqlite using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=doExpression_7373dbacf8
ROOST_METHOD_SIG_HASH=doExpression_f053663651

Scenario 1: Test NIN Expression Type Conversion

Details:
  TestName: ninExpressionTypeConvertsToNotInFormat
  Description: Verify that when an expression with NIN (Not In) type is passed, the method correctly transforms it into a NOT IN format by delegating to the doNot method with a wrapped IN expression.

Execution:
  Arrange: Create an Expression with ExpressionType.NIN, a Key as the left operand, and a list of values as the right operand. Initialize a StringBuilder context.
  Act: Invoke doExpression with the NIN expression and the StringBuilder context.
  Assert: Verify that the context contains "NOT " prefix followed by the converted IN expression format.

Validation:
  The assertion verifies that NIN expressions are properly transformed into "NOT <left> IN <right>" format instead of "<left> NOT IN <right>". This is significant because Neo4j requires this specific syntax for negated IN operations.

---

Scenario 2: Test EQ Expression Type Conversion

Details:
  TestName: eqExpressionTypeProducesEqualityOperator
  Description: Verify that an expression with EQ (Equals) type correctly produces the " = " operator symbol between the left and right operands.

Execution:
  Arrange: Create an Expression with ExpressionType.EQ, a Key as the left operand, and a value as the right operand. Initialize a StringBuilder context.
  Act: Invoke doExpression with the EQ expression and the StringBuilder context.
  Assert: Verify that the context contains the left operand, followed by " = ", followed by the right operand.

Validation:
  The assertion confirms that equality comparisons are properly formatted with the correct operator symbol. This is essential for generating valid Neo4j query conditions.

---

Scenario 3: Test NE Expression Type Conversion

Details:
  TestName: neExpressionTypeProducesNotEqualOperator
  Description: Verify that an expression with NE (Not Equals) type correctly produces the " <> " operator symbol between operands.

Execution:
  Arrange: Create an Expression with ExpressionType.NE, a Key as the left operand, and a value as the right operand. Initialize a StringBuilder context.
  Act: Invoke doExpression with the NE expression and the StringBuilder context.
  Assert: Verify that the context contains the left operand, followed by " <> ", followed by the right operand.

Validation:
  The assertion ensures that inequality comparisons use the correct Neo4j syntax. This is important for filtering records that do not match a specific value.

---

Scenario 4: Test LT Expression Type Conversion

Details:
  TestName: ltExpressionTypeProducesLessThanOperator
  Description: Verify that an expression with LT (Less Than) type correctly produces the " < " operator symbol.

Execution:
  Arrange: Create an Expression with ExpressionType.LT, a Key as the left operand, and a numeric value as the right operand. Initialize a StringBuilder context.
  Act: Invoke doExpression with the LT expression and the StringBuilder context.
  Assert: Verify that the context contains the left operand, followed by " < ", followed by the right operand.

Validation:
  The assertion confirms proper handling of less-than comparisons, which is crucial for range-based filtering in vector store queries.

---

Scenario 5: Test LTE Expression Type Conversion

Details:
  TestName: lteExpressionTypeProducesLessThanOrEqualOperator
  Description: Verify that an expression with LTE (Less Than or Equal) type correctly produces the " <= " operator symbol.

Execution:
  Arrange: Create an Expression with ExpressionType.LTE, a Key as the left operand, and a numeric value as the right operand. Initialize a StringBuilder context.
  Act: Invoke doExpression with the LTE expression and the StringBuilder context.
  Assert: Verify that the context contains the left operand, followed by " <= ", followed by the right operand.

Validation:
  The assertion verifies that inclusive lower-bound comparisons are correctly formatted for Neo4j queries.

---

Scenario 6: Test GT Expression Type Conversion

Details:
  TestName: gtExpressionTypeProducesGreaterThanOperator
  Description: Verify that an expression with GT (Greater Than) type correctly produces the " > " operator symbol.

Execution:
  Arrange: Create an Expression with ExpressionType.GT, a Key as the left operand, and a numeric value as the right operand. Initialize a StringBuilder context.
  Act: Invoke doExpression with the GT expression and the StringBuilder context.
  Assert: Verify that the context contains the left operand, followed by " > ", followed by the right operand.

Validation:
  The assertion ensures proper handling of greater-than comparisons for range filtering operations.

---

Scenario 7: Test GTE Expression Type Conversion

Details:
  TestName: gteExpressionTypeProducesGreaterThanOrEqualOperator
  Description: Verify that an expression with GTE (Greater Than or Equal) type correctly produces the " >= " operator symbol.

Execution:
  Arrange: Create an Expression with ExpressionType.GTE, a Key as the left operand, and a numeric value as the right operand. Initialize a StringBuilder context.
  Act: Invoke doExpression with the GTE expression and the StringBuilder context.
  Assert: Verify that the context contains the left operand, followed by " >= ", followed by the right operand.

Validation:
  The assertion confirms that inclusive upper-bound comparisons are correctly formatted.

---

Scenario 8: Test IN Expression Type Conversion

Details:
  TestName: inExpressionTypeProducesInOperator
  Description: Verify that an expression with IN type correctly produces the " IN " operator symbol for membership testing.

Execution:
  Arrange: Create an Expression with ExpressionType.IN, a Key as the left operand, and a list of values as the right operand. Initialize a StringBuilder context.
  Act: Invoke doExpression with the IN expression and the StringBuilder context.
  Assert: Verify that the context contains the left operand, followed by " IN ", followed by the right operand list.

Validation:
  The assertion verifies that IN expressions are properly formatted for checking if a value exists within a set of values.

---

Scenario 9: Test AND Expression Type Conversion

Details:
  TestName: andExpressionTypeProducesAndOperator
  Description: Verify that an expression with AND type correctly produces the " AND " operator symbol for logical conjunction.

Execution:
  Arrange: Create an Expression with ExpressionType.AND, with two sub-expressions as left and right operands. Initialize a StringBuilder context.
  Act: Invoke doExpression with the AND expression and the StringBuilder context.
  Assert: Verify that the context contains the left expression, followed by " AND ", followed by the right expression.

Validation:
  The assertion ensures that compound conditions using logical AND are properly formatted for Neo4j queries.

---

Scenario 10: Test OR Expression Type Conversion

Details:
  TestName: orExpressionTypeProducesOrOperator
  Description: Verify that an expression with OR type correctly produces the " OR " operator symbol for logical disjunction.

Execution:
  Arrange: Create an Expression with ExpressionType.OR, with two sub-expressions as left and right operands. Initialize a StringBuilder context.
  Act: Invoke doExpression with the OR expression and the StringBuilder context.
  Assert: Verify that the context contains the left expression, followed by " OR ", followed by the right expression.

Validation:
  The assertion confirms that compound conditions using logical OR are properly formatted.

---

Scenario 11: Test Expression With Key Containing Special Characters

Details:
  TestName: expressionWithKeyContainingQuotesHandledCorrectly
  Description: Verify that when a Key contains quote characters, they are properly handled during conversion.

Execution:
  Arrange: Create an Expression with ExpressionType.EQ, a Key containing quote characters as the left operand, and a value as the right operand. Initialize a StringBuilder context.
  Act: Invoke doExpression with the expression and the StringBuilder context.
  Assert: Verify that the context properly formats the key with quotes removed as per the doKey implementation.

Validation:
  The assertion ensures that special characters in key names do not break the query syntax, maintaining query integrity.

---

Scenario 12: Test NIN Expression With Multiple Values

Details:
  TestName: ninExpressionWithMultipleValuesConvertsCorrectly
  Description: Verify that a NIN expression with multiple values in the right operand is correctly transformed to NOT IN format.

Execution:
  Arrange: Create an Expression with ExpressionType.NIN, a Key as the left operand, and a list containing multiple values as the right operand. Initialize a StringBuilder context.
  Act: Invoke doExpression with the NIN expression and the StringBuilder context.
  Assert: Verify that the context contains "NOT " followed by the properly formatted IN expression with all values.

Validation:
  The assertion confirms that NIN expressions with multiple values are correctly negated, which is important for excluding multiple values from query results.

---

Scenario 13: Test Expression Appends To Existing Context

Details:
  TestName: expressionAppendsToExistingContextContent
  Description: Verify that when the StringBuilder context already contains content, the expression is appended rather than replacing existing content.

Execution:
  Arrange: Create an Expression with ExpressionType.EQ. Initialize a StringBuilder context with pre-existing content.
  Act: Invoke doExpression with the expression and the pre-populated StringBuilder context.
  Assert: Verify that the original content remains and the new expression is appended after it.

Validation:
  The assertion ensures that the method correctly builds upon existing query fragments, which is essential for constructing complex compound queries.

---

Scenario 14: Test Expression With Null Right Operand Value

Details:
  TestName: expressionWithNullValueHandledByConvertOperand
  Description: Verify that an expression where the right operand is null is processed through the convertOperand method.

Execution:
  Arrange: Create an Expression with ExpressionType.EQ, a Key as the left operand, and null as the right operand. Initialize a StringBuilder context.
  Act: Invoke doExpression with the expression and the StringBuilder context.
  Assert: Verify that the method processes the null value according to the convertOperand implementation.

Validation:
  The assertion confirms that null values are handled appropriately, which is important for queries that need to check for null/missing values.

---

Scenario 15: Test NOT Expression Type Through getOperationSymbol

Details:
  TestName: notExpressionTypeReturnsNotOperatorSymbol
  Description: Verify that when an expression with NOT type is processed through the else branch, it produces the " NOT " operator symbol.

Execution:
  Arrange: Create an Expression with ExpressionType.NOT with appropriate operands. Initialize a StringBuilder context.
  Act: Invoke doExpression with the NOT expression and the StringBuilder context.
  Assert: Verify that the context contains the appropriate NOT operator formatting.

Validation:
  The assertion ensures that NOT expressions that don't go through the NIN special case are still handled correctly via the getOperationSymbol method.

*/

// ********RoostGPT********

package org.springframework.ai.vectorstore.neo4j.filter;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.ai.vectorstore.filter.Filter;
import org.springframework.ai.vectorstore.filter.Filter.Expression;
import org.springframework.ai.vectorstore.filter.Filter.Key;
import org.springframework.ai.vectorstore.filter.Filter.Value;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.ai.vectorstore.filter.Filter.Group;
import org.springframework.ai.vectorstore.filter.converter.AbstractFilterExpressionConverter;

class Neo4JVectorFilterExpressionConverterDoExpressionTest {

	private Neo4jVectorFilterExpressionConverter converter;

	@BeforeEach
	void setUp() {
		converter = new Neo4jVectorFilterExpressionConverter();
	}

	@Test
	@Tag("valid")
	void ninExpressionTypeConvertsToNotInFormat() {
		// Arrange
		Key key = new Key("status");
		Value value = new Value(List.of("active", "pending"));
		Expression ninExpression = new Expression(Filter.ExpressionType.NIN, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(ninExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains("NOT"), "Result should contain NOT prefix");
	}

	@Test
	@Tag("valid")
	void eqExpressionTypeProducesEqualityOperator() {
		// Arrange
		Key key = new Key("name");
		Value value = new Value("test");
		Expression eqExpression = new Expression(Filter.ExpressionType.EQ, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(eqExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" = "), "Result should contain equality operator");
	}

	@Test
	@Tag("valid")
	void neExpressionTypeProducesNotEqualOperator() {
		// Arrange
		Key key = new Key("category");
		Value value = new Value("archived");
		Expression neExpression = new Expression(Filter.ExpressionType.NE, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(neExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" <> "), "Result should contain not equal operator");
	}

	@Test
	@Tag("valid")
	void ltExpressionTypeProducesLessThanOperator() {
		// Arrange
		Key key = new Key("age");
		Value value = new Value(30);
		Expression ltExpression = new Expression(Filter.ExpressionType.LT, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(ltExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" < "), "Result should contain less than operator");
	}

	@Test
	@Tag("valid")
	void lteExpressionTypeProducesLessThanOrEqualOperator() {
		// Arrange
		Key key = new Key("score");
		Value value = new Value(100);
		Expression lteExpression = new Expression(Filter.ExpressionType.LTE, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(lteExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" <= "), "Result should contain less than or equal operator");
	}

	@Test
	@Tag("valid")
	void gtExpressionTypeProducesGreaterThanOperator() {
		// Arrange
		Key key = new Key("price");
		Value value = new Value(50.0);
		Expression gtExpression = new Expression(Filter.ExpressionType.GT, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(gtExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" > "), "Result should contain greater than operator");
	}

	@Test
	@Tag("valid")
	void gteExpressionTypeProducesGreaterThanOrEqualOperator() {
		// Arrange
		Key key = new Key("quantity");
		Value value = new Value(10);
		Expression gteExpression = new Expression(Filter.ExpressionType.GTE, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(gteExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" >= "), "Result should contain greater than or equal operator");
	}

	@Test
	@Tag("valid")
	void inExpressionTypeProducesInOperator() {
		// Arrange
		Key key = new Key("type");
		Value value = new Value(List.of("A", "B", "C"));
		Expression inExpression = new Expression(Filter.ExpressionType.IN, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(inExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" IN "), "Result should contain IN operator");
	}

	@Test
	@Tag("valid")
	void andExpressionTypeProducesAndOperator() {
		// Arrange
		Expression leftExpr = new Expression(Filter.ExpressionType.EQ, new Key("field1"), new Value("value1"));
		Expression rightExpr = new Expression(Filter.ExpressionType.EQ, new Key("field2"), new Value("value2"));
		Expression andExpression = new Expression(Filter.ExpressionType.AND, leftExpr, rightExpr);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(andExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" AND "), "Result should contain AND operator");
	}

	@Test
	@Tag("valid")
	void orExpressionTypeProducesOrOperator() {
		// Arrange
		Expression leftExpr = new Expression(Filter.ExpressionType.EQ, new Key("status"), new Value("active"));
		Expression rightExpr = new Expression(Filter.ExpressionType.EQ, new Key("status"), new Value("pending"));
		Expression orExpression = new Expression(Filter.ExpressionType.OR, leftExpr, rightExpr);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(orExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" OR "), "Result should contain OR operator");
	}

	@Test
	@Tag("boundary")
	void expressionWithKeyContainingQuotesHandledCorrectly() {
		// Arrange
		Key key = new Key("field\"name");
		Value value = new Value("testValue");
		Expression expression = new Expression(Filter.ExpressionType.EQ, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(expression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertFalse(result.isEmpty(), "Result should not be empty");
	}

	@Test
	@Tag("valid")
	void ninExpressionWithMultipleValuesConvertsCorrectly() {
		// Arrange
		Key key = new Key("category");
		Value value = new Value(List.of("cat1", "cat2", "cat3", "cat4"));
		Expression ninExpression = new Expression(Filter.ExpressionType.NIN, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(ninExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains("NOT"), "Result should contain NOT for NIN expression");
	}

	@Test
	@Tag("valid")
	void expressionAppendsToExistingContextContent() {
		// Arrange
		Key key = new Key("field");
		Value value = new Value("value");
		Expression expression = new Expression(Filter.ExpressionType.EQ, key, value);
		StringBuilder context = new StringBuilder("WHERE ");
		// Act
		converter.doExpression(expression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.startsWith("WHERE "), "Result should preserve existing content");
		assertTrue(result.contains(" = "), "Result should contain the appended expression");
	}

	@Test
	@Tag("boundary")
	void expressionWithNullValueHandledByConvertOperand() {
		// Arrange
		Key key = new Key("nullableField");
		Value value = new Value(null);
		Expression expression = new Expression(Filter.ExpressionType.EQ, key, value);
		StringBuilder context = new StringBuilder();
		// Act & Assert
		assertDoesNotThrow(() -> converter.doExpression(expression, context));
		String result = context.toString();
		assertNotNull(result);
	}

	@Test
	@Tag("valid")
	void notExpressionTypeReturnsNotOperatorSymbol() {
		// Arrange
		Expression innerExpression = new Expression(Filter.ExpressionType.EQ, new Key("active"), new Value(true));
		Expression notExpression = new Expression(Filter.ExpressionType.NOT, innerExpression, null);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(notExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" NOT "), "Result should contain NOT operator");
	}

	@Test
	@Tag("valid")
	void eqExpressionWithStringValueFormatsCorrectly() {
		// Arrange
		Key key = new Key("title");
		Value value = new Value("Hello World");
		Expression expression = new Expression(Filter.ExpressionType.EQ, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(expression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.length() > 0, "Result should not be empty");
	}

	@Test
	@Tag("valid")
	void eqExpressionWithNumericValueFormatsCorrectly() {
		// Arrange
		Key key = new Key("count");
		Value value = new Value(42);
		Expression expression = new Expression(Filter.ExpressionType.EQ, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(expression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" = "), "Result should contain equality operator");
	}

	@Test
	@Tag("valid")
	void eqExpressionWithBooleanValueFormatsCorrectly() {
		// Arrange
		Key key = new Key("isActive");
		Value value = new Value(true);
		Expression expression = new Expression(Filter.ExpressionType.EQ, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(expression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains(" = "), "Result should contain equality operator");
	}

	@Test
	@Tag("boundary")
	void inExpressionWithEmptyListHandled() {
		// Arrange
		Key key = new Key("tags");
		Value value = new Value(List.of());
		Expression inExpression = new Expression(Filter.ExpressionType.IN, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		assertDoesNotThrow(() -> converter.doExpression(inExpression, context));
		// Assert
		String result = context.toString();
		assertNotNull(result);
	}

	@Test
	@Tag("valid")
	void ninExpressionWithSingleValueConvertsCorrectly() {
		// Arrange
		Key key = new Key("status");
		Value value = new Value(List.of("deleted"));
		Expression ninExpression = new Expression(Filter.ExpressionType.NIN, key, value);
		StringBuilder context = new StringBuilder();
		// Act
		converter.doExpression(ninExpression, context);
		// Assert
		String result = context.toString();
		assertNotNull(result);
		assertTrue(result.contains("NOT"), "Result should contain NOT for NIN expression");
	}

}