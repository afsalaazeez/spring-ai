/*
 * Copyright 2023-2024 the original author or authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test spring-ai-nov using AI Type Claude AI and AI Model claude-haiku-4-5-20251001

ROOST_METHOD_HASH=doExpression_7373dbacf8
ROOST_METHOD_SIG_HASH=doExpression_f053663651

Scenario 1: NIN Expression Type Conversion to NOT IN

Details:
  TestName: ninExpressionConvertedToNotInExpression
  Description: This test verifies that when the doExpression method receives an expression with type NIN (Not In), it correctly converts it to a NOT IN expression by delegating to the doNot method with a transformed expression that wraps the original left and right operands in an IN expression.

Execution:
  Arrange: Create a mock Expression object with type NIN, set up left operand as a Key expression and right operand as a collection value. Create a StringBuilder to capture the context output. Initialize the Neo4jVectorFilterExpressionConverter instance.
  Act: Invoke doExpression method with the NIN expression and the StringBuilder context.
  Assert: Verify that the StringBuilder contains the NOT operator followed by the IN operation result. Confirm that doNot method was invoked internally with the transformed expression.

Validation:
  This test ensures that NIN expressions are properly converted to NOT IN format, which is essential for correct Neo4j query generation. The conversion logic must transform NIN into NOT(IN(...)) to maintain semantic correctness in the database query language.

*/

// ********RoostGPT********

package org.springframework.ai.vectorstore.neo4j.filter;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.CsvSource;
import org.mockito.ArgumentCaptor;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;
import org.mockito.Spy;
import org.springframework.ai.vectorstore.filter.Filter;
import org.springframework.ai.vectorstore.filter.Filter.Expression;
import org.springframework.ai.vectorstore.filter.Filter.Key;
import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.eq;
import static org.mockito.Mockito.*;
import org.junit.jupiter.api.*;
import org.springframework.ai.vectorstore.filter.Filter.Group;
import org.springframework.ai.vectorstore.filter.converter.AbstractFilterExpressionConverter;

@DisplayName("Neo4jVectorFilterExpressionConverter doExpression Tests")
class Neo4JVectorFilterExpressionConverterDoExpressionTest {

	@Spy
	private Neo4jVectorFilterExpressionConverter converter;

	@Mock
	private Expression mockExpression;

	@Mock
	private Expression mockLeftOperand;

	@Mock
	private Expression mockRightOperand;

	private StringBuilder context;

	@BeforeEach
	void setUp() {
		MockitoAnnotations.openMocks(this);
		context = new StringBuilder();
	}

	@Test
	@Tag("valid")
	@DisplayName("Should convert NIN expression type to NOT IN expression")
	void ninExpressionConvertedToNotInExpression() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.NIN);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, times(1)).doNot(any(Expression.class), eq(context));
		verify(mockExpression, atLeastOnce()).type();
		verify(mockExpression, atLeastOnce()).left();
		verify(mockExpression, atLeastOnce()).right();
	}

	@Test
	@Tag("valid")
	@DisplayName("Should handle EQ expression type with standard conversion")
	void eqExpressionTypeHandledWithStandardConversion() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.EQ);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" = ");
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, times(1)).convertOperand(mockLeftOperand, context);
		verify(converter, times(1)).convertOperand(mockRightOperand, context);
		verify(converter, times(1)).getOperationSymbol(mockExpression);
		verify(converter, never()).doNot(any(), any());
	}

	@Test
	@Tag("valid")
	@DisplayName("Should handle NE expression type with standard conversion")
	void neExpressionTypeHandledWithStandardConversion() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.NE);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" <> ");
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, times(1)).convertOperand(mockLeftOperand, context);
		verify(converter, times(1)).convertOperand(mockRightOperand, context);
		verify(converter, times(1)).getOperationSymbol(mockExpression);
	}

	@Test
	@Tag("valid")
	@DisplayName("Should handle GT expression type with standard conversion")
	void gtExpressionTypeHandledWithStandardConversion() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.GT);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" > ");
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, times(1)).convertOperand(mockLeftOperand, context);
		verify(converter, times(1)).convertOperand(mockRightOperand, context);
		verify(converter, times(1)).getOperationSymbol(mockExpression);
	}

	@Test
	@Tag("valid")
	@DisplayName("Should handle LT expression type with standard conversion")
	void ltExpressionTypeHandledWithStandardConversion() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.LT);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" < ");
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, times(1)).convertOperand(mockLeftOperand, context);
		verify(converter, times(1)).convertOperand(mockRightOperand, context);
		verify(converter, times(1)).getOperationSymbol(mockExpression);
	}

	@Test
	@Tag("valid")
	@DisplayName("Should handle GTE expression type with standard conversion")
	void gteExpressionTypeHandledWithStandardConversion() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.GTE);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" >= ");
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, times(1)).convertOperand(mockLeftOperand, context);
		verify(converter, times(1)).convertOperand(mockRightOperand, context);
		verify(converter, times(1)).getOperationSymbol(mockExpression);
	}

	@Test
	@Tag("valid")
	@DisplayName("Should handle LTE expression type with standard conversion")
	void lteExpressionTypeHandledWithStandardConversion() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.LTE);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" <= ");
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, times(1)).convertOperand(mockLeftOperand, context);
		verify(converter, times(1)).convertOperand(mockRightOperand, context);
		verify(converter, times(1)).getOperationSymbol(mockExpression);
	}

	@Test
	@Tag("valid")
	@DisplayName("Should handle IN expression type with standard conversion")
	void inExpressionTypeHandledWithStandardConversion() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.IN);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" IN ");
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, times(1)).convertOperand(mockLeftOperand, context);
		verify(converter, times(1)).convertOperand(mockRightOperand, context);
		verify(converter, times(1)).getOperationSymbol(mockExpression);
		verify(converter, never()).doNot(any(), any());
	}

	@Test
	@Tag("valid")
	@DisplayName("Should append operation symbol between operands for non-NIN expressions")
	void operationSymbolAppendedBetweenOperands() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.EQ);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" = ");
		doAnswer(invocation -> {
			StringBuilder sb = invocation.getArgument(1);
			sb.append("left");
			return null;
		}).when(converter).convertOperand(mockLeftOperand, context);
		doAnswer(invocation -> {
			StringBuilder sb = invocation.getArgument(1);
			sb.append("right");
			return null;
		}).when(converter).convertOperand(mockRightOperand, context);
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		assertEquals("left = right", context.toString());
	}

	@Test
	@Tag("boundary")
	@DisplayName("Should handle expression with null context gracefully")
	void expressionWithNullContextThrowsException() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.EQ);
		// Act & Assert
		assertThrows(NullPointerException.class, () -> {
			converter.doExpression(mockExpression, null);
		});
	}

	@Test
	@Tag("boundary")
	@DisplayName("Should handle expression with null expression object")
	void nullExpressionObjectThrowsException() {
		// Act & Assert
		assertThrows(NullPointerException.class, () -> {
			converter.doExpression(null, context);
		});
	}

	@Test
	@Tag("valid")
	@DisplayName("Should correctly invoke doNot for NIN with proper expression wrapping")
	void ninExpressionProperlyWrappedInNotExpression() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.NIN);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		ArgumentCaptor<Expression> expressionCaptor = ArgumentCaptor.forClass(Expression.class);
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter).doNot(expressionCaptor.capture(), eq(context));
		Expression capturedExpression = expressionCaptor.getValue();
		assertNotNull(capturedExpression);
		assertEquals(Filter.ExpressionType.NOT, capturedExpression.type());
	}

	@Test
	@Tag("valid")
	@DisplayName("Should handle multiple consecutive expressions")
	void multipleConsecutiveExpressionsHandledCorrectly() {
		// Arrange
		Expression expr1 = mock(Expression.class);
		Expression expr2 = mock(Expression.class);
		when(expr1.type()).thenReturn(Filter.ExpressionType.EQ);
		when(expr2.type()).thenReturn(Filter.ExpressionType.NE);
		when(expr1.left()).thenReturn(mockLeftOperand);
		when(expr1.right()).thenReturn(mockRightOperand);
		when(expr2.left()).thenReturn(mockLeftOperand);
		when(expr2.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(expr1)).thenReturn(" = ");
		when(converter.getOperationSymbol(expr2)).thenReturn(" <> ");
		// Act
		converter.doExpression(expr1, context);
		converter.doExpression(expr2, context);
		// Assert
		verify(converter, times(2)).convertOperand(any(), any());
		verify(converter, times(2)).getOperationSymbol(any());
	}

	@Test
	@Tag("valid")
	@DisplayName("Should preserve context state across multiple operations")
	void contextStatePreservedAcrossOperations() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.EQ);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" = ");
		context.append("prefix_");
		doAnswer(invocation -> {
			StringBuilder sb = invocation.getArgument(1);
			sb.append("operand");
			return null;
		}).when(converter).convertOperand(any(), any());
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		assertTrue(context.toString().startsWith("prefix_"));
	}

	@ParameterizedTest
	@Tag("valid")
	@DisplayName("Should handle various expression types correctly")
	@CsvSource({ "EQ, ' = '", "NE, ' <> '", "GT, ' > '", "LT, ' < '", "GTE, ' >= '", "LTE, ' <= '", "IN, ' IN '" })
	void variousExpressionTypesHandledCorrectly(String expressionType, String operationSymbol) {
		// Arrange
		Filter.ExpressionType type = Filter.ExpressionType.valueOf(expressionType);
		when(mockExpression.type()).thenReturn(type);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(operationSymbol);
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, times(1)).convertOperand(mockLeftOperand, context);
		verify(converter, times(1)).convertOperand(mockRightOperand, context);
		verify(converter, times(1)).getOperationSymbol(mockExpression);
		verify(converter, never()).doNot(any(), any());
	}

	@Test
	@Tag("valid")
	@DisplayName("Should not invoke doNot for non-NIN expression types")
	void doNotNotInvokedForNonNinExpressions() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.EQ);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" = ");
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, never()).doNot(any(), any());
	}

	@Test
	@Tag("valid")
	@DisplayName("Should invoke convertOperand exactly twice for non-NIN expressions")
	void convertOperandInvokedExactlyTwiceForNonNinExpressions() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.GT);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" > ");
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, times(2)).convertOperand(any(), eq(context));
	}

	@Test
	@Tag("valid")
	@DisplayName("Should invoke getOperationSymbol exactly once for non-NIN expressions")
	void getOperationSymbolInvokedExactlyOnceForNonNinExpressions() {
		// Arrange
		when(mockExpression.type()).thenReturn(Filter.ExpressionType.LTE);
		when(mockExpression.left()).thenReturn(mockLeftOperand);
		when(mockExpression.right()).thenReturn(mockRightOperand);
		when(converter.getOperationSymbol(mockExpression)).thenReturn(" <= ");
		// Act
		converter.doExpression(mockExpression, context);
		// Assert
		verify(converter, times(1)).getOperationSymbol(mockExpression);
	}

}